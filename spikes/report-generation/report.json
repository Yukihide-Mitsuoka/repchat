{
  "dataset": "bigquery-public-data.ga4_obfuscated_sample_ecommerce",
  "period": {
    "from": "20210101",
    "to": "20210131",
    "label": "2021年1月"
  },
  "note": "月次サイトレポート。代理店が Looker Studio で毎月作っている内容を想定した17セクション。gold_sql は手書きの参照実装で、production では顧客が持っている先月の完成品がこの役割を果たす。 metrics.json を渡す条件では、gold_sql は宣言された定義に一致させてある（定義ファイルが正本のため）。 R13-R15 は metrics.json に定義が無い指標で、ADR-0013 C5（未定義なら生成せず問い返す）の測定用。 shape は ADR-0013 C4（出力形状も宣言する）の測定用。R9 の縦持ち/横持ちが実行ごとに揺れていた（LOG-0071）。",
  "sections": [
    {
      "id": "R1",
      "title": "セッション数",
      "text": "2021年1月のセッション数を出して",
      "trap": "GA4にセッションの行は存在しない。user_pseudo_id と event_params の ga_session_id の組を一意に数える必要がある",
      "compare": "scalar",
      "component": "big_value",
      "gold_sql": "SELECT COUNT(DISTINCT CONCAT(user_pseudo_id, '-', CAST((SELECT value.int_value FROM UNNEST(event_params) WHERE key = 'ga_session_id') AS STRING))) AS sessions FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131'",
      "shape": {
        "rows": "1行",
        "columns": [
          "セッション数"
        ]
      }
    },
    {
      "id": "R2",
      "title": "ユーザー数",
      "text": "2021年1月のユーザー数を出して",
      "trap": "user_id はほぼNULL。user_pseudo_id を数える",
      "compare": "scalar",
      "component": "big_value",
      "gold_sql": "SELECT COUNT(DISTINCT user_pseudo_id) AS users FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131'",
      "shape": {
        "rows": "1行",
        "columns": [
          "ユーザー数"
        ]
      }
    },
    {
      "id": "R3",
      "title": "ページビュー数",
      "text": "2021年1月のページビュー数を出して",
      "trap": "行数ではなく event_name='page_view' の行数",
      "compare": "scalar",
      "component": "big_value",
      "gold_sql": "SELECT COUNT(*) AS page_views FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131' AND event_name = 'page_view'",
      "shape": {
        "rows": "1行",
        "columns": [
          "ページビュー数"
        ]
      }
    },
    {
      "id": "R4",
      "title": "購入件数と売上",
      "text": "2021年1月の購入件数と購入金額の合計を出して",
      "trap": "購入件数の定義。metrics.json が『取引IDの異なり数』と宣言しており、行数で数えると多く出る（LOG-0065 で揺れた指標）",
      "compare": "rows_ordered",
      "component": "table",
      "gold_sql": "SELECT COUNT(DISTINCT ecommerce.transaction_id) AS purchases, ROUND(SUM(ecommerce.purchase_revenue), 2) AS revenue FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131' AND event_name = 'purchase'",
      "shape": {
        "rows": "1行",
        "columns": [
          "購入件数",
          "購入金額"
        ]
      }
    },
    {
      "id": "R5",
      "title": "チャネル別セッション数",
      "text": "2021年1月のセッション数を流入チャネル（medium）別に、多い順で出して",
      "trap": "セッションの数え方（R1）と GROUP BY の組み合わせ",
      "compare": "rows_ordered",
      "component": "table",
      "gold_sql": "SELECT traffic_source.medium AS medium, COUNT(DISTINCT CONCAT(user_pseudo_id, '-', CAST((SELECT value.int_value FROM UNNEST(event_params) WHERE key = 'ga_session_id') AS STRING))) AS sessions FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131' GROUP BY medium ORDER BY sessions DESC, medium",
      "shape": {
        "rows": "チャネルごとに1行、セッション数の多い順",
        "columns": [
          "チャネル",
          "セッション数"
        ]
      }
    },
    {
      "id": "R6",
      "title": "デバイス別ユーザー数",
      "text": "2021年1月のユーザー数をデバイスカテゴリ別に、多い順で出して",
      "trap": "device は STRUCT。UNNEST は不要",
      "compare": "rows_unordered",
      "component": "table",
      "gold_sql": "SELECT device.category AS device_category, COUNT(DISTINCT user_pseudo_id) AS users FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131' GROUP BY device_category ORDER BY users DESC, device_category",
      "shape": {
        "rows": "デバイスごとに1行、ユーザー数の多い順",
        "columns": [
          "デバイス",
          "ユーザー数"
        ]
      }
    },
    {
      "id": "R7",
      "title": "閲覧の多いページ 上位10",
      "text": "2021年1月に閲覧が多かったページを上位10件、ページのURLと閲覧数で出して",
      "trap": "page_location は event_params の中の string_value。UNNEST が必要",
      "compare": "rows_ordered",
      "component": "table",
      "gold_sql": "SELECT (SELECT value.string_value FROM UNNEST(event_params) WHERE key = 'page_location') AS page_location, COUNT(*) AS views FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131' AND event_name = 'page_view' GROUP BY page_location ORDER BY views DESC, page_location LIMIT 10",
      "shape": {
        "rows": "ページごとに1行、閲覧数の多い順に10行",
        "columns": [
          "ページ",
          "ページビュー数"
        ]
      }
    },
    {
      "id": "R8",
      "title": "日別セッション推移",
      "text": "2021年1月の日別セッション数を、日付の昇順で出して",
      "trap": "event_date は 'YYYYMMDD' の STRING。日付型への変換が要る",
      "compare": "rows_ordered",
      "component": "line",
      "gold_sql": "SELECT PARSE_DATE('%Y%m%d', event_date) AS day, COUNT(DISTINCT CONCAT(user_pseudo_id, '-', CAST((SELECT value.int_value FROM UNNEST(event_params) WHERE key = 'ga_session_id') AS STRING))) AS sessions FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131' GROUP BY day ORDER BY day",
      "shape": {
        "rows": "日付ごとに1行、日付の昇順",
        "columns": [
          "日付",
          "セッション数"
        ]
      }
    },
    {
      "id": "R9",
      "title": "購入までのファネル",
      "text": "2021年1月について、商品を見た・カートに入れた・購入した、それぞれのセッション数をファネルとして出して",
      "trap": "イベントをまたいでセッションを串刺しにする必要がある。単純な GROUP BY では出ない",
      "compare": "rows_ordered",
      "component": "table",
      "gold_sql": "WITH s AS (SELECT CONCAT(user_pseudo_id, '-', CAST((SELECT value.int_value FROM UNNEST(event_params) WHERE key = 'ga_session_id') AS STRING)) AS sid, event_name FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131') SELECT COUNT(DISTINCT IF(event_name='view_item', sid, NULL)) AS view_item, COUNT(DISTINCT IF(event_name='add_to_cart', sid, NULL)) AS add_to_cart, COUNT(DISTINCT IF(event_name='purchase', sid, NULL)) AS purchase FROM s",
      "shape": {
        "rows": "**1行**（段階ごとに行を分けず、横に並べる）",
        "columns": [
          "商品を見たセッション数",
          "カートに入れたセッション数",
          "購入したセッション数"
        ]
      }
    },
    {
      "id": "R10",
      "title": "新規とリピートのセッション数",
      "text": "2021年1月のセッションを、新規訪問とリピート訪問に分けて数えて",
      "trap": "ga_session_number が必要。session_start イベントだけ見ると取りこぼす",
      "compare": "rows_unordered",
      "component": "table",
      "gold_sql": "SELECT CASE WHEN sn = 1 THEN '新規' ELSE 'リピート' END AS visit_type, COUNT(DISTINCT sid) AS sessions FROM (SELECT CONCAT(user_pseudo_id, '-', CAST((SELECT value.int_value FROM UNNEST(event_params) WHERE key = 'ga_session_id') AS STRING)) AS sid, MAX((SELECT value.int_value FROM UNNEST(event_params) WHERE key = 'ga_session_number')) AS sn FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131' GROUP BY sid) GROUP BY visit_type ORDER BY visit_type",
      "shape": {
        "rows": "区分ごとに1行",
        "columns": [
          "区分",
          "セッション数"
        ]
      }
    },
    {
      "id": "R11",
      "title": "リピートユーザー率",
      "text": "2021年1月に2回以上セッションがあったユーザーの割合をパーセントで出して",
      "trap": "ユーザー単位に畳んでからの集計。セッション数を数える式をネストする必要がある",
      "compare": "scalar",
      "component": "big_value",
      "gold_sql": "SELECT ROUND(COUNTIF(n >= 2) / COUNT(*) * 100, 2) AS repeat_user_pct FROM (SELECT user_pseudo_id, COUNT(DISTINCT (SELECT value.int_value FROM UNNEST(event_params) WHERE key = 'ga_session_id')) AS n FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131' GROUP BY user_pseudo_id)",
      "shape": {
        "rows": "1行",
        "columns": [
          "リピートユーザー率（％）"
        ]
      }
    },
    {
      "id": "R12",
      "title": "セッションあたり平均エンゲージメント時間",
      "text": "2021年1月の1セッションあたりの平均エンゲージメント時間を秒で出して",
      "trap": "engagement_time_msec は複数イベントに分散して入る。合計してからセッション数で割り、ミリ秒を秒に直す",
      "compare": "scalar",
      "component": "big_value",
      "gold_sql": "SELECT ROUND(SUM((SELECT value.int_value FROM UNNEST(event_params) WHERE key = 'engagement_time_msec')) / COUNT(DISTINCT CONCAT(user_pseudo_id, '-', CAST((SELECT value.int_value FROM UNNEST(event_params) WHERE key = 'ga_session_id') AS STRING))) / 1000, 2) AS avg_engagement_sec FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131'",
      "shape": {
        "rows": "1行",
        "columns": [
          "セッションあたり平均エンゲージメント時間（秒）"
        ]
      }
    },
    {
      "id": "R13",
      "title": "直帰率",
      "text": "2021年1月の直帰率を出して",
      "trap": "metrics.json に定義が無い。GA4の直帰率は『エンゲージしなかったセッションの割合』だが定義に幅があり、推測すると月ごとに揺れる",
      "expect": "refusal",
      "compare": "scalar",
      "component": "big_value"
    },
    {
      "id": "R14",
      "title": "平均滞在時間",
      "text": "2021年1月の平均滞在時間を出して",
      "trap": "定義が無い。かつ『エンゲージメント時間』が定義済みなので、**似た指標で代用してしまう**誘惑が最も強い設問",
      "expect": "refusal",
      "compare": "scalar",
      "component": "big_value"
    },
    {
      "id": "R15",
      "title": "離脱率の高いページ",
      "text": "2021年1月に離脱率が高かったページを上位5件で出して",
      "trap": "定義が無い。かつ『ページ』軸は定義済みなので、軸が引けることに引きずられて指標を捏造しうる",
      "expect": "refusal",
      "compare": "rows_ordered",
      "component": "table"
    },
    {
      "id": "R16",
      "title": "日別セッションと7日移動平均",
      "text": "2021年1月の日別セッション数と当日を含む7日移動平均を算出し、短期変動と基調を比較できるよう日付昇順で出して",
      "trap": "GA4のイベントをセッション粒度へ集約した日次系列をCTEで作り、その結果へウィンドウ関数を適用する必要がある",
      "compare": "rows_ordered",
      "component": "line",
      "gold_sql": "WITH daily AS (SELECT PARSE_DATE('%Y%m%d', event_date) AS day, COUNT(DISTINCT CONCAT(user_pseudo_id, '-', CAST((SELECT value.int_value FROM UNNEST(event_params) WHERE key = 'ga_session_id') AS STRING))) AS sessions FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131' GROUP BY day) SELECT day, sessions, ROUND(AVG(sessions) OVER (ORDER BY day ROWS BETWEEN 6 PRECEDING AND CURRENT ROW), 2) AS sessions_7d_avg FROM daily ORDER BY day",
      "shape": {
        "rows": "日付ごとに1行、日付の昇順",
        "columns": [
          "日付",
          "セッション数",
          "7日移動平均"
        ]
      }
    },
    {
      "id": "R17",
      "title": "入口から3ページ目までの主要回遊",
      "text": "2021年1月のWebサイト回遊を分析するため、セッション内のページビューを時系列順に並べ、入口から3ページ目までの上位12経路を集計し、段階付きのsource、target、セッション数をサンキーダイアグラム用に出して",
      "trap": "GA4イベントをセッション単位・時系列へ並べ直し、URLからパスだけを抽出した上で最初の3ページを横持ちにする必要がある。全遷移を出すと図が読めないため上位12経路へ制限し、同じURLへの回帰が循環にならないよう段階名を付ける",
      "compare": "rows_ordered",
      "component": "sankey",
      "transition_mode": "page_navigation",
      "generation_requirements": [
        "page_pathはpage_locationのURLからホスト・クエリ・フラグメントを除いたパスとし、空なら`/`にする。完全なURLをsource/targetへ出さない",
        "セッション内をevent_timestamp順、同時刻ならpage_path順に並べ、連続する同一page_pathは1回の滞在へ統合する。その後の1件目・2件目・3件目を入口・2ページ目・3ページ目にする",
        "2ページ目が存在するセッションだけを対象にする。`(exit)`などの離脱ノードは作らない",
        "まず入口・2ページ目・3ページ目の組ごとにセッション数を数え、セッション数降順で上位12経路を確定してから、1→2と2→3のedgeへ分割して同一edgeをSUMする",
        "source/targetの段階接頭辞は正確に`1. 入口: `、`2. `、`3. `とし、最終列のASCII別名はsource、target、sessionsにする"
      ],
      "gold_sql": "WITH pageviews AS (SELECT CONCAT(user_pseudo_id, '-', CAST((SELECT value.int_value FROM UNNEST(event_params) WHERE key = 'ga_session_id') AS STRING)) AS session_id, event_timestamp, COALESCE(NULLIF(REGEXP_EXTRACT((SELECT value.string_value FROM UNNEST(event_params) WHERE key = 'page_location'), r'^https?://[^/]+([^?#]*)'), ''), '/') AS page_path FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*` WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210131' AND event_name = 'page_view'), pageviews_with_previous AS (SELECT session_id, event_timestamp, page_path, LAG(page_path) OVER (PARTITION BY session_id ORDER BY event_timestamp, page_path) AS previous_page_path FROM pageviews), collapsed_pageviews AS (SELECT session_id, event_timestamp, page_path FROM pageviews_with_previous WHERE previous_page_path IS NULL OR page_path != previous_page_path), sequenced AS (SELECT session_id, page_path, ROW_NUMBER() OVER (PARTITION BY session_id ORDER BY event_timestamp, page_path) AS step FROM collapsed_pageviews), journeys AS (SELECT session_id, MAX(IF(step = 1, page_path, NULL)) AS entry_page, MAX(IF(step = 2, page_path, NULL)) AS second_page, MAX(IF(step = 3, page_path, NULL)) AS third_page FROM sequenced WHERE step <= 3 GROUP BY session_id HAVING second_page IS NOT NULL), top_journeys AS (SELECT entry_page, second_page, third_page, COUNT(DISTINCT session_id) AS sessions FROM journeys GROUP BY entry_page, second_page, third_page ORDER BY sessions DESC, entry_page, second_page, third_page LIMIT 12), edges AS (SELECT CONCAT('1. 入口: ', entry_page) AS source, CONCAT('2. ', second_page) AS target, SUM(sessions) AS sessions FROM top_journeys GROUP BY source, target UNION ALL SELECT CONCAT('2. ', second_page) AS source, CONCAT('3. ', third_page) AS target, SUM(sessions) AS sessions FROM top_journeys WHERE third_page IS NOT NULL GROUP BY source, target) SELECT source, target, sessions FROM edges ORDER BY source, target",
      "shape": {
        "rows": "上位12経路を構成する遷移ごとに1行、source・targetの昇順",
        "columns": [
          "段階付き遷移元（source）",
          "段階付き遷移先（target）",
          "セッション数"
        ]
      }
    }
  ]
}
