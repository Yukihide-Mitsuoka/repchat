---
id: decision-log
title: Decision Log
authority: 4
read_when: [architecture-change, planning, onboarding]
---

# Decision Log

Append-only index of decisions. Newest first. Two kinds of entries:

- **ADR-linked**: architectural decisions — full context lives in `docs/adr/`.
- **Lightweight**: decisions too small for an ADR but worth remembering (COD-052).

Rules: never edit or delete past entries; supersede with a new entry that references the
old one. One line per entry. AI agents append entries in the same PR as the change.

| Date | ID | Decision | Link |
|------|----|----------|------|
| 2026-08-02 | LOG-0094 | Issue #188の最初の非GA4縦切りにBitcoin公開取引を選定した。`bigquery-public-data.crypto_bitcoin.transactions`はGA4と異なる語彙で、`outputs ARRAY<STRUCT>`内の`addresses ARRAY`を二段階で展開できる。ライブデモはprofile IDから許可datasetと期間契約をサーバー側解決し、2024年月次の受取アドレス数帯別取引数だけを既知値未照合で実行可能にする。基準SQLのBigQuery dry runは3,115,504,440 bytes（約2.91GiB）で、実送信は20GiB hard limitと費用再確認を維持する。公開1種類、独立レビューなし、実値照合なしのためIssue #188は閉じず、任意schema対応を表明しない | [Issue #188](https://github.com/Yukihide-Mitsuoka/repchat/issues/188), [spike](../spikes/report-generation/README.md) |
| 2026-08-02 | LOG-0093 | ADR-0018の既存区分を具体化し、組織コンテキストを分析レシピ・指標定義・user表示嗜好から分離する。tenantまたは分析対象組織の事業モデル、収益構造、会計年度、戦略目標と目標階層、意思決定周期、事業制約、固有用語、禁止解釈を、owner・source・根拠・適用日・見直し日を持つ承認済みpolicy revisionとして扱う。#180のKPI提案と#181の所見は同じrevisionを参照し、期限切れまたは取得不能時は組織前提を推測しない。AIは候補を作れるが自動有効化しない | [requirements](../docs/requirements/adaptive-analysis-memory.md), [Issue #257](https://github.com/Yukihide-Mitsuoka/repchat/issues/257) |
| 2026-08-02 | LOG-0092 | 直接親lockを`ef70594`から最終first-parent commit `74d9255`（ai-dev-foundation release 1.5.2）へ進め、PR #238/#241の継承内容とPR #244の保護workflowに対応する受理来歴を完成させる。子固有CHANGELOG、製品実装、GitHub設定、クラウド状態は変更しない | [Issue #246](https://github.com/Yukihide-Mitsuoka/repchat/issues/246) |
| 2026-08-02 | LOG-0091 | 直接親lockを`919508c`から次のfirst-parent commit `ef70594`（ai-dev-foundation #152）へ進める。旧Scorecard compositeの削除と回帰テストはPR #241で、保護されたdirect callerはPR #244で受理済みのため、実装を二重化せず受理済み状態の来歴を記録する | [Issue #246](https://github.com/Yukihide-Mitsuoka/repchat/issues/246) |
| 2026-08-02 | LOG-0090 | 直接親lockを`854b885`から次のfirst-parent commit `919508c`（ai-dev-foundation #149）へ進める。fleet artifactを既存の継承rootへ収める最終内容はPR #238/#241で受理済みであり、保護されたMakefileや中間配置へ戻さず来歴だけを前進させる | [Issue #246](https://github.com/Yukihide-Mitsuoka/repchat/issues/246) |
| 2026-08-02 | LOG-0089 | 直接親lockを`cf4ebed`から次のfirst-parent commit `854b885`（ai-dev-foundation release 1.5.1）へ進める。親差分は保護されたFoundation CHANGELOGだけで、PR #238/#241で受理済みの最終継承blobとPR #244のScorecard workflowを変更せず、来歴だけを順序どおり記録する | [Issue #246](https://github.com/Yukihide-Mitsuoka/repchat/issues/246) |
| 2026-08-02 | LOG-0088 | 直接親lockを`c3b5dbf`から次のfirst-parent commit `cf4ebed`（ai-dev-foundation #147）へ進める。PR #238/#241で`74d9255`までの最終継承blobを、PR #244で保護されたScorecard workflowを既に受理済みのため、中間commitの内容へ戻さずlockだけを更新する | [Issue #246](https://github.com/Yukihide-Mitsuoka/repchat/issues/246) |
| 2026-08-02 | LOG-0087 | 適応型分析メモリーの既定保持を、有効方針は置換・削除まで、未承認候補30日、構造化feedback 90日、旧revision・承認・取消監査365日、解約後30日で完全削除、raw conversation・query result bodyは0日とする。design partnerの要望を確認してtenant契約単位で変更できる。本番ロール・認証は重い独立課題としてデモ後の専用grill-meでIssue #194を確定し、現在のデモ構築は止めない | [requirements](../docs/requirements/adaptive-analysis-memory.md), [Issue #194](https://github.com/Yukihide-Mitsuoka/repchat/issues/194) |
| 2026-08-02 | ADR-0018 (accepted) | repository ownerが、元の実装案を維持することではなく、誤適用を防ぐ設計の核心に同意した。顧客固有の分析修正を生の会話履歴やmodel weightへ暗黙学習させず、scope・authority・不変revision・由来・期限を持つ分析方針としてPostgresを正本にする。AIは候補を作るが有効版を黙って上書きせず、組織・指標・security方針は権限者承認、個人の低リスク表示嗜好も「次回も」の明示と通知・取消を必要とする。「初回」は固定判定せず、tenant、分析対象、schema版、目的、読者、指標定義版、recipe版のfingerprintでexact/compatible/newを決める。類似度は認可に使わない。Issue #160の`proceed`、#179/#188、#180の分析仕様revision後に手動統制のPhase 1から開始し、候補抽出、反復改善、派生retrieval indexの順に実測で段階導入する | [ADR-0018](../docs/adr/0018-govern-adaptive-analysis-memory.md), [requirements](../docs/requirements/adaptive-analysis-memory.md), [Issue #220](https://github.com/Yukihide-Mitsuoka/repchat/issues/220) |
| 2026-08-02 | LOG-0086 | 受理済み基盤ADR-0014の直接子migrate段階として、`.ai/guardrails.md`を重複した保護対象ルール本文から基盤所有の薄いadapterへ切り替える。正本`contracts/foundation/guardrails.md`を通常同期し、RepChat固有の強化は宣言済みproject overlayだけに置き、完全に受理した親commitを`c3b5dbf`へ進める | [Issue #211](https://github.com/Yukihide-Mitsuoka/repchat/issues/211) |
| 2026-08-01 | LOG-0085 | 受理済み基盤ADR-0014に従い、汎用`bugfix` skillをRepChat固有の保護対象から基盤所有の同期対象へ移す。今後の同型バグ探索規約は通常のreviewed Template Syncで受け取り、project-owned回帰テストで所有権・transport・探索契約を固定する | [Issue #208](https://github.com/Yukihide-Mitsuoka/repchat/issues/208) |
| 2026-08-01 | LOG-0084 | 受理済み基盤ADR-0014の直接子移行として、誤って同期されたai-dev-foundation自身のproject profileとoverlayをRepChat固有の保護済みprofile・overlayへ置き換え、manifest v2でfoundation contractの後にRepChatの識別情報とstack factsだけを読み込む | [Issue #201](https://github.com/Yukihide-Mitsuoka/repchat/issues/201) |
| 2026-07-30 | ADR-0017 (proposed) | Slackを独立した分析botにせず、既存の分析仕様・SQL生成・検証・実行・ArtifactBundle・build・immutable revisionへ接続する任意の認可付きadapterにする。Webをdashboard・SQL・指標定義・来歴の正本UIとし、Slackは許可channelでの相談、進捗、要約、任意のgraph、認証付きlinkに限定する。`team_id + channel_id`とSlack userの両方をtenant・RepChat userへ対応付け、workspaceをtenant境界にしない。Slack投稿はdata exportとして既定link-only、graph・数値はopt-in、SQLとprotected linkの汎用unfurlは禁止する。自由質問はIssue #180と#188の後、検証済みrevision通知は分離して先行できる。Slack自体を差別化とは表現しない | [ADR-0017](../docs/adr/0017-use-slack-as-an-authorized-analysis-interface.md), [requirements](../docs/requirements/slack-analysis-interface.md) |
| 2026-07-30 | LOG-0083 | **公開GA4での6/6・15/15はschema固有の実証であり、未知の独自nested/repeated schemaへの一般化を証明しない**。TheLook 12/12も未知のrelational schemaであってこの穴を埋めない。GA4語彙・公開例を流用しない複数schemaで、`UNNEST`、複数階層、join、window、funnel、metadata不足を含む反復benchmarkを行い、独立review済み参照結果に対する一致、誤推測、確認・拒否、scan上限を測る。Issue #188に合格するまで任意schema対応を製品能力として表明せず、Slack等からの自由質問も実行しない | [Issue #188](https://github.com/Yukihide-Mitsuoka/repchat/issues/188), [requirements §9](../docs/requirements.md) |
| 2026-07-29 | ADR-0016 (accepted) | repository ownerがPR #168をマージし、同一repositoryのbot、規定branch、main base、直接親の完全SHAをすべて検証できる機械的Template Syncだけをhard size limitの限定例外にする決定を承認した | [ADR-0016](../docs/adr/0016-authenticate-oversized-foundation-sync-prs.md) |
| 2026-07-29 | ADR-0016 (proposed) | 通常PRのGR-020を維持したまま、同一repositoryのbot、規定branch、main base、直接親の完全SHAをすべて検証できる機械的Template Syncだけをhard size limitの限定例外にする | [ADR-0016](../docs/adr/0016-authenticate-oversized-foundation-sync-prs.md) |
| 2026-07-29 | LOG-0082 | **ADR-0014の顧客所有方針を維持し、配送方式をADR-0015としてオーナー承認**。LOG-0077の「顧客repositoryだけでEvidenceが動く」は不正確だったため訂正する。顧客Gitにはページ・SQL・manifestだけを置き、固定rendererと組み合わせて再構築する。GitHubは生成・build時だけ使い、閲覧経路へ入れない。顧客Gitとmanaged fallbackは同じArtifactBundle・検証・build・有効化pipelineを使い、保存先adapterだけを分ける。GitHub接続はselected-repository限定のGitHub App、短期installation token、最小権限とし、PAT・共有deploy keyは使わない。build成功後だけcommit SHAと`report_version`を有効化し、失敗時は直前の成功版を配信する。初期は専用private repository、App管理branchへの直接commit、手動オンボーディングを推奨し、PR modeは実需が出るまで延期する | [ADR-0015](../docs/adr/0015-publish-artifacts-through-customer-git.md), [ADR-0014訂正](../docs/adr/0014-who-owns-the-generated-artifacts.md) |
| 2026-07-28 | LOG-0081 | **LOG-0078の「デモを見せられる形」を完了**。`make demo PROJECT=<project>` 1コマンドで、隔離venvとpin済みPython依存の用意、pin済みEvidence公式scaffold＋最小lockfile、`npm ci`とcritical監査、実Vertex AI＋BigQueryでの生成・既知値照合、12ソースのmaterialize、build、dev serverとブラウザ起動まで行う。費用確認を必須にし、非対話実行は`ACCEPT_COST=yes`を要求、`DRY_RUN=yes`はファイル・クラウドを変更しない。**実環境受入**（`kotonoha-bi-dev`）: r1〜r12 materialize成功、HTTP 200、ブラウザで月次レポートとセッション118,380・ユーザー94,790.0・PV419,004・購入895・売上57.4k、および未定義3指標の拒否を確認。ブラウザerror 0、Evidence依存側の非致命warningあり。Python依存は`pip-audit`で既知脆弱性0。Evidence公式テンプレートを全コネクタのまま入れるとcritical 10件だったため不採用。必要な3パッケージだけにしVitestを修正版へoverrideして**critical 0 / high 16**。残るhighはlocalhost・信頼済み入力・非本番・実行時critical監査で緩和する。Looker Studio利用者向け5分説明も追加。**限界**: このローカルデモはgate・認証・executorを通らず、テナント越境不能の根拠は別の受入試験。次はデザインパートナーへ実際に見せ、未検証仮説を人間から測る | [demo](../docs/demo.md), [spike](../spikes/report-generation/README.md) |
| 2026-07-28 | LOG-0080 | **ADR-0013をオーナー承認によりacceptedへ変更**。C1〜C6はLOG-0065〜0073の実測どおり正式採用し、C7はacceptedなADR-0014で解決済み。**採用内容**: 指標定義は自前の層、顧客環境の平坦化ビューは既定で不採用、固定するのは意味であってSQL構文ではない、意味と出力形状を宣言、未定義語は問い返し、生成結果は既知の数値と照合。これにより`src/`移植前のADR承認不足は解消 | [ADR-0013](../docs/adr/0013-metric-definitions-live-in-our-own-layer.md) |
| 2026-07-28 | LOG-0079 | **初期の主要顧客を、複数の顧客企業へ分析・定期レポートを提供する日本の小規模代理店またはソフトウェアベンダーに確定。直販は残すが、初期の主経路ではなくフォールバック**。その先の利用企業は、DWHにデータと技術担当者がいるがデータ専任者はいない日本の中小企業。**理由**: LOG-0064で痛みが観測された経路、`vendors → tenants → users` の既存境界、代理店が持つ指標知識の顧客間再利用、最初の3〜5社へ届く流通が一致する。対象企業だけを直販先として定義すると、流通と指標定義の担い手が未解決になる。mission.md の主要顧客は更新し、残る未決を課金区分と認証方式の2点に限定 | [mission](mission.md), [positioning §0/§6](../docs/positioning.md) |
| 2026-07-28 | LOG-0078 | **`src/` 移植より先に「デモを見せられる形にする」をやる（オーナー決定）。併せて引き継ぎ用に手順と落とし穴を文書化**。**決定の理由**: 移植は大きく後戻りしにくい一方、positioning §5 が残した唯一の未検証仮説「**BIを入れたが使われていない**」は**実装では潰せない**。今あるものは既に実データで動くデモ（LOG-0072〜0076）なので、**人に見せられる状態が先**。**受入条件を status.md §0.1 に明記**（(1) チェックアウト直後に1コマンドでブラウザにレポートが開く、(2) Looker Studio利用者に5分で違いが伝わる説明資料）。**デモのために `src/` を触らない**。**引き継ぎで埋めた穴**: Evidenceアプリは**リポジトリ外の作業ディレクトリにあり、セッションが変わると消える**ため、**scaffold からビルドまでの手順が誰にも再現できない状態**だった —— `npx degit evidence-dev/template` → `npm install @evidence-dev/bigquery` → `out/sources/ga4` と `out/pages/*.md` をコピー → `npm run sources && npm run build` を spike README に記載。**この手順を1コマンドにまとめることが、次の作業の実体**。**status.md §0.2 に「手を動かす前に知っておくこと」を新設**（実環境は稼働中で課金される／`make destroy` は `ALLOW_DESTROY` 必須かつ**確認コマンドを破棄コマンドより先に提示**／レポート生成1回 約¥2／ADC必須／venv共用）。**踏みやすい落とし穴も実際に踏んだものだけ列挙**（`spike:` はPR typeに無く `test:` を使う／**動いたと検証したを混同しない**（LOG-0060のキャッシュ配信）／**プラン成功は実行成功ではない**（LOG-0067）／測っていないことは測っていないと書く（GR-042）／mission.md を根拠にしない） | [docs/status.md](../docs/status.md) |
| 2026-07-28 | LOG-0077 | **ADR-0014 を accepted に。G1（3分割）とG4（顧客固有指標の例外）は推奨どおり承認、G2はオーナー判断で覆り、生成物は既定で顧客のリポジトリに置く**。**採った形**: リポジトリは顧客のもの、**書き込むのはこちら**（LookMLと同じ、**所有と操作を分ける**）。顧客はGitを操作しない。**著者の反対理由が消えた理由**: 反対の根拠は「顧客側にリポジトリと**PRを理解する人**が要る」だったが、**それは操作を顧客にやらせる場合の話**であり、所有と操作を分ければ成立しない。**残る前提条件は「リポジトリとアプリ権限の付与＝IT承認1回」であって技能の要求ではない** —— 著者は前提条件の中身を取り違えていた。**成立の根拠（実測）**: LOG-0073 の2分割で `sources/*.sql` に入るのは**具体的なウェアハウスSQL**、ページは `select * from ga4.<id>` で読むだけであり、**指標定義は生成時の入力であって実行時の依存ではない**。したがって**顧客リポジトリにある一式だけでEvidenceが動く**＝「解約しても資産が残る」が比喩でなく事実になる。**残る代償**: (1) ビルドが顧客リポジトリの到達性に依存する（**倒れる方向はfail closed** —— 配信済みページは残り新規生成が止まるだけ）、(2) Git provider連携の実装が要り**経路が2本**（顧客リポジトリ／こちらホストのフォールバック）、(3) コミット単位（直接コミットかPRか）は未決で既定は直接コミット。**フォールバックを残す理由**: 既定を顧客側にしても間口を狭めないため（Lookerも同じ逃げ道を持つ）。**副次**: positioning §2.9（未決）が決着。同時に §2.1 を書き直した —— **見出しが「最優先」と言い本文が「必須条件ではない」と言う矛盾**を抱えており、**「売る相手を1業種に絞る」（採らない）と「業種知識をテンプレートで持つ」（採るが後回し）が混ざっていた**。2.7 を採った時点で位置づけが変わったのに見出しを直していなかったのが原因 | [docs/adr/0014](../docs/adr/0014-who-owns-the-generated-artifacts.md) |
| 2026-07-28 | LOG-0076 | **生成ページを2テナントへ実際に配信。同一シェル・別々の数値・再ビルド無しを視覚的に確認**（LOG-0075 で構造だけ確認して残していた宿題を消化）。`tenant_serve.py` が、**1度だけビルドしたシェル**にテナント別 `.arrow` を被せて2ポートで配る。**結果**: `:8801` desktop が セッション**68,649** / PV**244,577**、`:8802` mobile が **47,088** / **165,210**。**シェルのHTMLは byte 同一**（両ポートで sha256 一致）で、**差し替えた2件だけが変わり他は同一**（ユーザー数94,790・購入895は両テナントで同じ）＝意図した箇所だけが効いている。これは **LOG-0025 の Option 2＝ADR-0005 の②結果キャッシュを Evidence 側の境界で表現したもの**であり、**生成ページに対しても成立することが実測になった**。**代表していないこと**: (1) 12クエリ中**2件だけ**差し替えた（どちらもスカラーで `_length` が変わらず `_data` の入替だけで済む。表や時系列は行数が変わるため `_columns`/`_length` も要る）、(2) **テナント境界は `device.category` で代用**しており本番でexecutorが注入するテナント述語ではない（**測ったのは配信の仕組みであってバインダではない**。バインダは LOG-0042 に独立した証明がある）、(3) **gate も認証も経路に入っていない**（ポート番号がテナントを決めているだけ） | [spikes/report-generation/](../spikes/report-generation/README.md) |
| 2026-07-28 | LOG-0075 | **生成ページにもテナント別配信の継ぎ目があることを確認し、LOG-0073 の診断を1件訂正**。次の一手（シェル/データ分離との結線）の前提確認。**LOG-0025 は手書きページでこの継ぎ目を実証**しており、**生成ページでも同じかは未確認**だった。ビルド成果物を分解した結果**同じ構造**: `build/api/<page>/…/all-queries.json` が `r1_columns`/`r1_length`/`r1_data` → ハッシュを**36キー**持ち、`build/api/prerendered_queries/<hash>.arrow` が初期表示の実データ（38本）。**36キーすべてが実ファイルに解決**（未解決0）し、中身は検証済みの値と一致（`r1_data: {'sessions': [118380.0]}`、`r4_data: {'purchases': [895.0], 'revenue': [57350.0]}`）。→ **生成ページもクエリ単位でデータを差し替えられる**ので、LOG-0025 の **Option 2（テナント別結果を同じハッシュURLで配る＝ADR-0005 の②結果キャッシュ）**が生成物に対しても成立する。**訂正**: LOG-0073 で `94,790.0` の小数表示を**「Evidence側の表示形式の推定」**としたが**不正確**だった。`.arrow` の中身自体が float（`118380.0`）で、**データがページに届く時点で既に float**。描画時の推測ではないので、**`fmt=` を足しても根本は直らない** —— SQL側のキャストか、Evidence のパイプライン（parquet→DuckDB→Arrow）の型の扱いを見る必要がある。**未実施**: 2テナント分のデータを実際に配って別々の数値が描画されることは未確認（LOG-0025 は手書きページで視覚的に実証済み、生成ページでは構造の確認まで）。**あわせて依存関係の見立ても訂正**: 「C7 が結線の形を決めるので先」としたが、**再検討すると結線は C7 に依存しない**（どちらの案でもビルドするのはこちら側で、成果物の置き場所はビルドの入力元の話）。依存を過大に見ていた | [spikes/report-generation/](../spikes/report-generation/README.md) |
| 2026-07-28 | LOG-0074 | **ADR-0014 起草（proposed）— 生成物と定義の所有を3種類に分けて扱う**。ADR-0013 C7 が先送りした唯一の論点で、positioning §2.9 の衝突（**「資産が顧客に残る」という売り文句** vs **「非エンジニアでも使える」**）をそのまま引き取った。**G1（3分割）が中核**: 生成物と定義は性質の違う3種類で、**同じ場所に置く必然性がない** —— **ページ**（顧客の成果物）、**ソース定義**（顧客スキーマ依存、LOG-0073 で2種類に分かれると実測）、**指標定義**（代理店が書き顧客間で使い回す、LOG-0071 で顧客数に比例しないと実測）。**G2〜G4 は著者の推奨であって決定ではない**（オーナー判断で覆ってよい旨をファイル冒頭に明記）。**G2 の推奨**: ページの置き場所は**既定でこちら＋取り出し保証**。理由は、**LOG-0064 の痛みは *repoがどこにあるか* ではなく *担当者が辞めると消えること* だった**ため。顧客のGitを既定にすると**Gitを持たない顧客をオンボーディングできず**、positioning §0 の対象顧客（技術者1人、データ専任者なし）と衝突する。顧客リポジトリへの継続ミラーは**望む顧客向けの上乗せ**とし、その場合も**顧客はPRを操作しなくてよい**（**所有と操作を分ける**）。**G4 の推奨**: 指標定義は**顧客のGitに置いてはならない** —— 置くと指標追加のたびに**代理店が全顧客分を更新して回る**ことになり、LOG-0071 で確認した利点を自分で捨て、D3 が禁じた形になる。顧客固有指標のみ例外。**正直な代償も記録**: 「あなたのGitにあります」という最も強い訴求は使えなくなり、言えるのは「いつでも取り出せます／担当者が辞めても消えません」まで。**D6 の注意**: こちらがホストする以上**こちらが落ちれば取り出せない**ので、書き出し機能が動いていることの確認が保守として必要。**オーナーが決めること3点を明示**（3分割の是非／G2の既定＝訴求の強さとオンボーディングの間口のトレードオフ／G4の例外の可否） | [docs/adr/0014](../docs/adr/0014-who-owns-the-generated-artifacts.md) |
| 2026-07-28 | LOG-0073 | **生成したレポートを Evidence で実際に描画。実データで表示されることを目視確認**（positioning §5 の「次は設計ではなくデモ」に対応。生成物をビルドして見た人がいなかった）。**構造上の発見: 生成器は2成果物を出す必要がある**。Evidence はソース定義（`sources/<name>/*.sql`）でウェアハウスに問い合わせ、**ページSQLは自前のDuckDB層**でその結果を参照する設計のため、**ページにBigQueryのSQLを直接埋めても動かない**（`_TABLE_SUFFIX` や `UNNEST` をDuckDBが解釈しようとする）。`out/sources/ga4/<id>.sql`（ウェアハウスSQL）と `out/pages/*.md`（`select * from ga4.<id>` ＋コンポーネント）に分離し、**拒否されたセクションはソースファイルを作らない**（定義漏れが空のグラフに化けないため）。**認証は鍵不要**: Evidence の BigQuery コネクタは **`authenticator: gcloud-cli`** で ADC を使え、**鍵ファイルが要らない**。WrenAI のコネクタが SA鍵必須で D1 と衝突した（LOG-0068）のと対照的で、**Evidence を選んでいたことがここでも効いた**。**結果**: 12本すべて materialize、ビルド通過、**実データで描画**（セッション118,380 / PV419,004 / 購入895・売上57.4k / organic 41.1k、いずれも検証済みの値と一致）。**文言の誤りを修正**: 拒否セクションが「SQLを生成できませんでした」と表示されていたが**これは嘘**で、生成失敗ではなく**定義の欠落**が理由。不具合に見えて再試行を誘うため、**未定義の語を名指しし、定義の追加を促す**文言に変更（C5が伝えるべきは機械の失敗ではなく定義の欠落）。**残る粗**: ユーザー数が `94,790.0` と小数表示（SQLは整数を返しており、**Evidence側の表示形式推定**。`fmt=` で解決可能だが未対応） | [spikes/report-generation/](../spikes/report-generation/README.md) |
| 2026-07-28 | LOG-0072 | **ADR-0013 C4（出力形状の宣言）を実装・実測。15/15 が2回。ただし「宣言の中身」を取り違えると壊れることが判明**。セクションごとに `shape`（列の並び＋行の単位）を宣言する形で実装。**C4の主張は成立** — 形状を宣言するとファネルの縦持ち/横持ちの揺れは止まり、4回の実行すべてで安定した。**しかし宣言しただけでは他の3セクションが壊れた**: モデルが**宣言した列名をSQLの識別子としてそのまま使った**ため、`AS 日付` は `Illegal input character`（BigQueryは非ASCII識別子を裸で受け付けない）、`リピートユーザー率（％）` は**バッククォートで囲んでも** `Invalid field name`（全角括弧はフィールド名に使えない）。→ **C4の宣言は「表示名と順序」であって「SQL識別子」ではない**。分離し、**SQLの別名はASCIIのsnake_case、表示名はレポート組み立て側が付ける**としたところ **2回連続で15/15**（未定義3件の拒否を含む）。**この分離は Evidence の使い方とも一致する** — コンポーネントは `title` を data と別に受け取るので、表示名をSQLに埋め込む必要がない。**測定装置の反省（4度目）**: `Invalid field name` が見えるまでに1往復無駄にした。`exec_bq` が例外の**先頭200文字**を返しており、BigQueryの `BadRequest` は**長いAPI URLから始まる**ため**エンドポイントだけ表示され理由が隠れていた**。例外から `errors[0].message` を取り出すよう修正。**このセッションで4度目の「自分で診断情報を捨てる」で、いずれも同じ形（出力を絞る・切る・握り潰す）** | [spikes/report-generation/](../spikes/report-generation/README.md) |
| 2026-07-28 | LOG-0071 | **C5の補正を検証 — 拒否の境界が正しくなり、残る不安定は出力形状だけになった**。LOG-0070 の過剰拒否は**2種類**あり、分けて対処した: **語の違い**（設問「新規訪問」／定義「新規セッション」）には定義に**別名**を持たせ、**合成**（「商品を見たセッション数」＝セッション数を `event_name` で絞ったもの）には**「定義済みの指標を宣言済みの列で絞り込む・分割するのは代用ではない」**という規則を追加。**結果（2回実行）**: 真に未定義な3語は**両方とも拒否**、言い換えと合成は**両方とも回答**、合計 14/15 と 15/15。**過剰受容は起きなかった** — `滞在時間` を「エンゲージメント時間」の別名に入れて「平均滞在時間」と衝突させたが**両実行とも拒否**しており、別名は境界を緩めなかった。**2回の差は R9 の縦持ち／横持ちのみで数値は一致** → **数値は安定し、残る不安定は出力形状に絞り込まれた**。これは **C4（出力形状も宣言する）が未実装だから**であり、C5 の問題ではない。**次の実装対象が C4 に確定**。**D3 の確認**: 別名は45個/12定義だが**これは GA4 の語彙**で、GA4を使う顧客なら**使い回せる**ため**標準化されたデータソースでは顧客数に比例しない**（顧客固有の業務データは未測定）。**正直な限界**: `新規訪問` は失敗した文字列そのものを別名に足しており **R10 が通ったのは循環**。**情報量があるのは R9（別名では解けず合成規則で解けた）と R13〜R15（別名を足しても拒否が維持された）** | [spikes/report-generation/](../spikes/report-generation/README.md) |
| 2026-07-28 | LOG-0070 | **ADR-0013 C5 を実測し、意図は成立・境界は誤りと判明**。C5 は起草時点で**ADR唯一の未実測の決定**だったため、実装より先に測った。定義に**わざと入れていない**指標3つ（直帰率・平均滞在時間・離脱率）を追加し、応答スキーマに `undefined_terms` を足して測定。**成立した側**: 真に未定義な3語は **3/3 で拒否**。とくに「平均滞在時間」は**定義済みの「エンゲージメント時間」がすぐ隣にあるのに代用しなかった** —— C5が最も効いてほしい場面で効いた。**誤っていた側**: **言い換えを未定義と判定して過剰拒否**。設問「新規訪問」に対し定義は「新規セッション」で、モデルは『定義済みの「新規セッション」で代用せず、未定義語として処理します』と**明示的に拒否**した。説明的な言い回し（ファネルの「商品を見たセッション数」）も同様。**しかも実行ごとに揺れる**（同じ設問が回によって通ったり拒否されたり＝**過剰拒否が非決定的**で、一貫した境界より悪い）。→ **C5 の判定基準は「語が字面で一致するか」ではなく「意味が定義に解決できるか」**であり、定義に**別名・言い換え**を持たせる必要がある（WrenAI が `knowledge/glossary/` を別立てにしているのは、おそらくこの理由。C1で参考にすると述べた形式に glossary が含まれる）。**次に測るべき未解決**: 別名で過剰拒否が解消するか、そして**別名の網羅が顧客数に比例しないか**（比例するなら D3 の再発）。**測定装置の反省も記録**: 最初の実行では過剰拒否が `no sql` としか出ず**生成失敗と区別できなかった**（`undefined_terms` と `reason` を記録して初めて原因が読めた）。**このセッションで3度目の「自分で診断情報を捨てる」**（空のbqエラー、`grep`で捨てたトレースバック、これ）。あわせて **ADCのプリフライト**を追加（期限切れが12セクション目でスタックトレースとして現れ「何も出力されない」と誤読していた。LOG-0056 で `deploy.sh` に入れたのと同型） | [spikes/report-generation/](../spikes/report-generation/README.md) |
| 2026-07-28 | LOG-0069 | **ADR-0013 起草 — 指標定義は自前の層に持ち、平坦化を顧客環境に持ち込まない**（C1〜C7）。LOG-0065〜0068 の実測から。**C1 WrenAI/MDL は不採用**（形式のみ参考）。Apache 2.0 なので法的障害はなく、**採らない理由は解けない問題が残ることと保守**。**C2 平坦化は自前の生成層。BigQueryビューは既定で使わない** — 顧客にビューを作らせる案の却下理由は、**保守工数が顧客数に比例する（D3が禁じた形の再発。比例するのがコネクタ数かビュー数かの違いしかない）**ことと、**顧客環境にあるため壊れても直せない**こと。書き込み権限を持つ案は**D1の読み取り専用性を失い、権限は放置で開く側に倒れる（D6違反）**。ビューは**アーキテクチャではなく個別最適化**に降格し、特定顧客で必要と測定できたときのみ検討。**起草時に著者は一度ビュー案を支持したが、保守工数の勘定が甘く撤回**（オーナーの「保守工数は堅牢性に含まれる」基準に照らして誤りだった）。**C3 固定するのは意味であって構文ではない** — SQL生成の決定性は要件から外れ、**定義の一意性が要件**（実装要件を1つ消した）。**C4 指標の意味と出力形状の両方を宣言**（ファネルの縦持ち/横持ち）。**C5 定義済みの語は再解釈させず、未定義の指標は生成せず問い返す（fail closed）** — 推測を許すと再現性がその1問だけ失われ、**読み手には定義済みと推測の区別がつかない**。**C6 使う前に既知の数値と照合**し、照合できないセクションは数値を出さず未検証と表示。**C7 定義の置き場所（顧客のGitか否か）は本ADRでは決めない** — C1〜C6 は「定義層がこちら側の1箇所であること」だけに依存し、所有者は別問題（positioning §2.9、独立ADR）。**適用範囲の限定**: 本ADRはGA4での実測に基づくため、**他データソースでMDLの制約が当てはまらないなら再評価してよい** | [docs/adr/0013](../docs/adr/0013-metric-definitions-live-in-our-own-layer.md) |
| 2026-07-28 | LOG-0068 | **LOG-0067 の「GA4で使えない理由」を訂正（結論は不変、精度が上がった）**。初版は「**ワイルドカード `events_*` が非対応**」としたが不正確だった。**ワイルドカードは `table_reference` に `table: events_*` と書けば通り**、生成される `` `project`.dataset.`events_*` `` という**分割バッククォートは BigQuery で実際に動く**（1日分22,096行で確認。**計画が通ることと動くことは別**なので実行して確かめた — LOG-0060 で同じ取り違えをしている）。**非対応なのは `ref_sql` の中だけ**（バッククォート識別子もワイルドカードも、引用符の有無を問わずパース失敗）。**正しい制約は「1つのMDL構文の中でワイルドカードと平坦化を両立できない」**: `ref_sql` は UNNEST を通すがワイルドカードを書けず、`table_reference`＋計算列はワイルドカードを書けるが **UNNEST を書けない** — 計算列の式は意味エンジンが識別子をモデルの宣言列に対して解決するため、`UNNEST` の中身（`key` / `value`）が見えず `source column not found: key` で失敗する（`event_params` を列として宣言しても解消しない）。**結論は不変**（GA4の平坦化はMDLの外＝BigQueryのビューに置く必要があり、書き込み権限とADR-0010 D1の衝突は残る）。**追加で判明**: WrenAI の BigQuery コネクタは `credentials`（サービスアカウント鍵）を必須とし ADC を受け付けない。**鍵を持たないことがD1の要点**なので、**向こうのコネクタを使うとD1が消える**。C案（定義層は借り、実行は自前）ではコネクタを使わないため問題にならないが、**採用範囲を「意味層まで」に限る根拠が1つ増えた** | [spikes/wrenai-evaluation/](../spikes/wrenai-evaluation/README.md) |
| 2026-07-28 | LOG-0067 | **WrenAI（Apache 2.0）を実機評価。定義層は借りるが、GA4では現状そのまま使えず、アクセス制御は預けられない**。LOG-0066 の直後に `metrics.json` が **MDL の劣化版を発明し始めていた**ため、自作の前に既存OSSを評価した。**Git管理は失われない**（MDLはYAMLファイル群、接続情報は `~/.wren/profiles.yml` に分離＝リポジトリ外。ロールバック・ブランチ・Actions・Secretsの4用途すべて成立）。**Docker不要**（`pip install wrenai`のCLI）。**LLMを持たないのは利点** — `wren ask` は薄いラッパーで、意味層・SQL計画・コネクタのみ提供しエージェントがCLIを駆動する設計。既存のGeminiパイプラインを置き換えず下に敷ける。**GA4では現状使えない**: `ref_sql` は wren-core 自身のパーサを通り、切り分けの結果 **バッククォート識別子と ワイルドカードテーブル `events_*` が非対応**（`table_reference` 方式と **相関UNNESTサブクエリは通る** — UNNESTを懸念していた当初の見立ては外れ）。GA4は日次シャードで月次レポートが `events_*`＋`_TABLE_SUFFIX` 前提のため、平坦化をMDLに書けない。**回避策はBigQuery側にビューを作ることだが、顧客プロジェクトへの書き込み権限が要り、ADR-0010 D1 の読み取り専用テナント別SAと衝突する**（採用前に決める必要あり）。**アクセス制御は預けられない**: README は RLAC/CLAC を謳うが**アーキテクチャ文書にもMDLリファレンスにも記載がなく**、確認できるのは「露出しない列は見えない」だけ。D6（ドリフト時にどちらへ倒れるかで採否を決める）より、**文書化されていない制御に境界を預けない**。**マルチテナントの記載も無し**（構造は1プロジェクト＋1アクティブプロファイルのCLI）＝control planeは引き続き必要。**Apache 2.0 での大幅改修は法的に問題なし**（改変部分を非公開のままSaaS提供可、義務はライセンス同梱・帰属保持・改変ファイルへの変更明記§4b・NOTICE引き継ぎ、特許許諾は特許訴訟で失効§3、**商標は非許諾§6で製品名に "Wren" を使えない**）。**実質的制約は法務ではなく保守** — AGPL-3.0が将来モジュール用に予約されており公開パッケージはマニフェスト優先のため版を固定して更新時に確認する運用が要る。活発開発中のものを大幅改修すれば更新を諦めるかリベースし続けるかになるため、**エンジンをフォークせず依存として使い周囲に自前の層を置く**。**未達**: GA4をMDLに載せられないため、`spikes/report-generation` の12問との直接比較は未実施 | [spikes/wrenai-evaluation/](../spikes/wrenai-evaluation/README.md) |
| 2026-07-27 | LOG-0066 | **LOG-0065の結論「定義しないと安定しない」を追試で裏付け、セマンティック層の要件が具体化**。ADRを書く前に、仮定のままだった因果を測定した。最小の定義層（`metrics.json`＝粒度・指標・軸の宣言。揺れていた購入件数と新規判定を含む）をプロンプトに渡し、同条件で3回実行。**結果: 11/12が3回とも同じ。数値は12/12で実行間完全一致**。定義なしでは購入件数が 1204↔895、新規判定が 29527↔28135 で揺れていたものが、それぞれ 895 / 28135 に固定された。**最も重要な発見: 定義が固定するのは意味であって構文ではない**。**SQLの文面は12中7で毎回変わっている**（別名・整形・CTEの有無）**のに、数値は12/12一致**。→ **生成SQLをキャッシュして固定する必要はなく、定義を固定すれば結果が決まる**。これはセマンティック層の実装要件を具体的にする（SQL生成の決定性ではなく、定義の一意性が要件）。**定義層の守備範囲の不足も判明**: 残る1件（ファネル）は3回とも同じ失敗で、**数値は完全一致のまま縦持ち/横持ちが参照と食い違った**。`metrics.json` は指標の意味を宣言するが**出力の形を宣言していない**。レポートとしてはどちらも正しく、決めておかないと決まらない。→ **セマンティック層は指標の定義と出力形状の両方を固定する必要がある**。実装は `--no-metrics` で両条件を1フラグで切り替えられる形にした。**限界**: 問い・参照SQL・定義ファイルをすべて同一人物が書いており、実顧客の定義は違いうる（それ自体がR4/R10の論点） | [spikes/report-generation/](../spikes/report-generation/README.md) |
| 2026-07-27 | LOG-0065 | **実データからレポート1枚を生成、9/12・¥1.36。§2.8の結論は維持し、理由を訂正**。日本語の記述12セクション→SQL生成→実行→手書き参照との照合→Evidenceページ1枚を、**GA4エクスポートの公開サンプル**（`bigquery-public-data.ga4_obfuscated_sample_ecommerce`＝リセラーの顧客が実際に持つのと同じスキーマ、顧客データは不使用）に対して実施。**訂正**: §2.8は「生イベントからのセッション化・`UNNEST`の重ね合わせは自然言語SQLが最も苦手な領域」を根拠にしていたが**実測で外れた**。`user_pseudo_id`×`ga_session_id`のセッション定義、`event_params`の`UNNEST`、`page_location`抽出、`purchase`行限定の売上（`items`の二重計上回避）、**ファネルのセッション串刺し**まで正しく書けた。**落ちたのは定義が決まっていない箇所だけ**: 購入件数は`COUNT(DISTINCT transaction_id)`=895 vs `COUNT(*)`=1204（GA4は同一取引でpurchaseが重複発火するため**モデルの方が妥当な可能性**）、新規判定は`LOGICAL_OR(event_name='first_visit')`=29527 vs `ga_session_number=1`=28135（**どちらもGA4の標準的定義**）。ファネルは数値完全一致で縦持ち/横持ちの差のみ。**決定的なのは非決定性**: `temperature=0`でも実行ごとに解釈が変わり、購入件数が回によって揺れた。**同じ問いで月ごとに数字が変わるレポートは月次レポートとして失格**。→ **セマンティック層が必須である理由は「難しいから」ではなく「定義しないと安定しないから」**。この違いは実装方針を変える（SQL生成の精度向上ではなく、定義の固定と再現性が要件）。**測定装置側の欠陥も記録**: 初回の失敗3件のうち2件はモデルではなく比較の作りが原因で、「列名はモデルの自由」と書きながら**グループ化のラベル文字列と出力の列数**を自由として扱っていなかった（`rows_unordered`の追加と、**本番で既存レポートが列構成を決めるのと同じく期待形状をプロンプトで渡す**ことで解消）。残る1件は参照SQLのタイムアウトで、**例外メッセージが空**のため診断不能だった（例外クラス名も記録するよう修正）。**正直な限界**: 問いも参照SQLも同一人物が書いており、一致は共通のバイアスかもしれない。**実顧客のレポートは違う定義を使っている可能性があり、それこそがR4/R10の論点** | [spikes/report-generation/](../spikes/report-generation/README.md) |
| 2026-07-27 | LOG-0064 | **仮説だった痛みが実観測で裏付けられ、優先順位と入口設計が確定**（GA4/Amplitude代理店での観測。社内の金額は記録しない、構造のみ）。**観測1（Looker Studio）**: レポート制作の共有アカウント廃止で移行コストが発生。顧客側でも**接続者・作成者の退職で資産が失われ**、権限管理も問題。**Pro（$9/人・月）でも閲覧範囲と権限の設計は容易でない**。→ **この構想の元々の動機（所有権・ガバナンス）を裏付ける**。**観測2（Amplitude）**: **解約の主因が「使いこなせない割に高い」**。Amplitudeは分析レポートを簡単に作れる製品なので、**「作るのが難しい」という説明が消える** — **変数を切り分ける自然実験**として機能し、残る説明は「**何を分析すべきか分からない**」。→ **LOG-0062の空欄（比較表で唯一どの列も埋まっていない行）を実観測が裏付けた**。**帰結1**: 答えは「もっと簡単なツール」ではない（Amplitudeより簡単にしても同じ理由で解約される）。**最初から入っている**か**生成される**かのどちらか。**帰結2**: **価格の基準が$9に引かれている** — 想定の月3,000〜6,000円は2〜4倍で正当化が要る。**帰結3**: 共有アカウント廃止は**期限のある強制イベント**で、乗り換えが起きるのは不満があるときではなく「どのみち変えねばならないとき」。ただし**移行を終えれば扉は閉じる**。**入口設計を確定**: 「何を見たいですか」と聞かない（分からない人は欲しいものも言えない）。**入力は顧客が毎月手作業で作っている既存レポート**とし、そこに何が重要かが既に埋め込まれている＝**顧客自身がドメイン知識を持ち込む**ため、**業種テンプレートは必須条件ではなく加速装置になる**（単独で始める上で大きい）。流れは「既存レポート→DWH接続→スキーマ対応づけをAIが提案し不明点だけ質問→SQLとEvidenceページ生成→**既知の数値1つで照合**→人が承認しコミット」。**手作業レポートを入力にすると入力と検証データが同時に手に入る**。**機構では差別化しない**（Omni agent等が既に持ちコモディティ化する）。**セマンティック層は選択肢ではなく必須**: Amplitudeの上流の作り込みは重いが**ファネル/リテンション/コホートに意味を与えていた**ものであり、DWHへ直接SQLを投げると意味も飛ぶ。生イベントからのセッション化・ウィンドウ関数・`UNNEST`は**自然言語SQLが最も苦手な領域**。売り物は「Amplitudeを安くSQLで置き換える」ではなく**「代理店が正しいと知っている分析が定義済みで入っていて、日本語で聞けて、資産が顧客に残る」**。**OEM経路を追加**: 単独の最大の障害は機能ではなく流通で、OEMはそれを迂回する。`vendors`→`tenants`→`users` と `vendor_keys` は元々この形のために作ったもので**休眠させようとした層が主役に戻る**（mission.mdの元の記述は書き換えでなく両立の可能性）。**代理店は流通だけでなく「何を見るべきか」の中身も持ってくる**。必ず来る反論は「Looker Studioでいいのでは」で、**直販でもOEMでも最初に来る**。相手は1社に限らず、利益相反の無い相手を選べる。**残る唯一の関門**: 「**今レポートを作っている人が、明日も作れるか**」。Looker Studioはドラッグ&ドロップで非エンジニアが作れるが、こちらは「自然言語→PR→承認」で**同じ人が同じようには作れない**。**他が全部揃ってもここが崩れれば売れない**。**次は設計ではなくデモ** | [docs/positioning.md](../docs/positioning.md) |
| 2026-07-27 | LOG-0063 | **ポジショニングを確定（何で戦い、何で戦わないか）**。LOG-0062の競合調査から導いた方針で、実装の優先順位はここから逆算する。**最大の切り分け**: **価格非公開は「日本のSMBを相手にしていない」という意味**で、Omni/Holistics/Zenlyticは土俵が違う（Zenlyticの顧客はVerizon等）。**実質的な競合は Evidence Cloud と Metabase＋OSSの2〜3系統だけ**であり、6社と戦う話ではない。この切り分けをしないと必要のない絶望をする。**続ける根拠**: インフラはゼロスケール構成で顧客ゼロの維持費がほぼゼロ＝**現金が燃えていない**ため、失うのは時間だけ。「顧客がいないなら作るな」は現金が燃えている場合の原則。**採る（優先順位順）**: (1) **業種を絞る** — 比較表で唯一空いている「何を見ればいいか分からない人」を埋めるにはAIが*正しい*ダッシュボードを作る必要があり、それは業種知識に依存する。横断製品は全業種に効くテンプレートを作れないので、**1業種に絞れば世界的な横断製品が構造的に降りてこられない**。単独であることを初めて有利に使える角度（業種は未定、ヒアリングで決める）。(2) **日本語ドキュメント** — 追加コストがほぼゼロの差別化は稀。かつサポート工数を下げる投資でもある。(3) **AIへのデータ受け渡しの説明責任** — 日本の情シスの問い「どのAIに渡りどこで処理されるか」に、LOG-0061の列単位宣言的マスキング＋モデル選択/BYOK/リージョン明示で答える。(4) **請求書払い** — 競合はカード自己申込のみ、個人事業主なら明日から可能。(5) **接続作業をサービスに含める** — セルフサーブ製品には原理的に不可能。(6) 価格は下げられる（$15/人を下回っても黒字）が**単独では堀にならない**。**採らない**: BI機能で追いつく／英語圏／価格だけ／Metabase型GUI探索UI（自然言語がGUIの代わり）／セキュリティ機能の網羅。**直視すべき帰結**: 選ばれる理由が機能でなくなるため**ソフトウェア事業よりサービス業に近い形**になり、founder自身が製品の一部になる。副業として成立するかはここにかかる。**未検証の仮説と判定条件も明記**（「BIを入れたが使われていない」が3/5社以上で裏付け、全員が使いこなせているなら構想は成立しない＝やめるのも正しい結論） | [docs/positioning.md](../docs/positioning.md) |
| 2026-07-27 | LOG-0062 | **競合調査（各社の公開ページを当日取得）。構想の技術要素はほぼ既存製品で埋まっていることが判明**し、以前の私の見立て2つを訂正する。**訂正1**: 「AIの出力をPRでレビューする製品を知らない」は誤り — **Zenlytic はセマンティック層がGitにありPRでレビューする**と明記、**Holistics も AML で定義しPR・ロールバック可能**。**訂正2**: 「小さな会社向けの安価でコード管理されたBIは手薄」も誤り — **Evidence Cloud の Team が $15/人/月で、行レベルセキュリティ・ページ単位アクセス制御・AIクレジット込み**。構想していた月3,000〜6,000円は**土台にしている製品の商用版より高く**、しかも向こうは当方が未実装の機能を既に売っている。**Holistics は構想（コード定義・PRレビュー・平易な言葉での質問・行列権限・テナント別セキュリティ）をほぼ全て備える**。加えて **Lightdash は $3,000/月の定額・席数無制限（OSS版無料）** で、席数課金そのものに業界的な逆風がある。**当てにできない差別化**: 自然言語SQL・セマンティック層・Git管理・行レベル権限（すべて複数社が保有）。**比較表で唯一どの列も埋まっていない行**（オーナーの指摘、別セッションでの議論を統合）: **既存製品が解いているのは「SQLが書けない」であって「何を見ればいいか分からない」ではない**。各社が競っているのは質問を投げる手段（自然言語・チャット・セマンティック層）だが、いずれも**利用者が何を聞くべきか分かっていることを前提**にしている。BIの席が導入後に使われなくなる典型的理由はSQLが書けないことではなく、**何を見ればよいか分からない**ことであり、**チャットUIを足しても前提は解消しない**。したがって**「自動でダッシュボード生成」は出力形式の話ではなく、「質問できない人」を利用者に含めるための手段**である（チャット＝作る側の入口、ダッシュボード＝使う側の入口）。Evidenceが永続ページを生成する土台であることとも整合する。**ただし未検証の仮説**: 「誰も見ないダッシュボード」はBIの古典的失敗そのもので、固定物にすれば解決するわけではなく**そのダッシュボードが正しいか**にかかる。スキーマだけからの生成は汎用的で価値が薄くなりがちで、正しさを左右する業務文脈は業種ごとに異なる（横断より業種を絞る方が成立しやすい可能性、これも未検証）。**Zenlyticが最も近い**（AIデータアナリストを標榜、定期AIダイジェストの追加は技術的に容易）ため**数ヶ月で埋まりうる差**。**残りうる差別化**: 上記の「質問できない人を含める」、日本語・日本市場（言語/サポート/請求書払い、上位製品が降りてこない帯）、接続が退職で壊れない設計（ADR-0010 D1、正面から売っている製品を見ない）。**結論**: 機能追加で追いつく方向は単独では成立しない。次の問いは「何を作るか」ではなく**「なぜ日本の小さな会社が Evidence Cloud や Metabase を使っていないのか」**であり、机上では答えが出ない。**検証の問いは最も聞きやすい形になる** — **「BIツールを入れたことはありますか。使われていますか。使われていないなら、なぜですか」**。導入済みで使われていない事例が複数出れば仮説は裏付けられ、**全員が使いこなせているならこの構想は成立しない**。売り込みが不要なので実績のない個人事業主でも成立する | [docs/competitive-landscape.md](../docs/competitive-landscape.md) |
| 2026-07-27 | LOG-0061 | **上位価格帯を売るための前提条件を、着手条件つきで記録**（AI MONBAN / DataSign のウェビナー資料を参考に整理）。中心的な認識は**「前提条件は一段ずつ上で繰り返される」**こと — 基本層（ダッシュボード）を売るにはテナント分離と接続主体の非属人化が要り、**上位層（AI分析レポート、月10,000〜15,000円/人想定）を売るには列レベルのデータ取り扱い制御が要る**。NL→SQLは**スキーマしかLLMに渡さない**が、分析レポート機能は**定義上、実データを渡す**ため。**決定的な接続**: この「マスキング」は status.md に未実装として残っていた**「列レベル制御」と同一物**であり、別課題として管理すると片方を作り直すことになる。RepChatは**列単位で意味が分かっているので検出ではなく宣言にできる**（汎用AIガバナンス製品が非構造テキストからのPII検出漏れを免れないのに対する構造的優位）。**採らない判断も記録**: (1) **OAuth接続を採らない** — 参考資料の接続画面に出ていた `OAuth token expired` は**この製品が解こうとしている失敗モードそのもの**（退職・期限切れで壊れる、Looker Studioと同根）で、ADR-0010 D1のサービスアカウント方式を維持する。設定の容易さと引き換えに中心的な売り文句を失うため。(2) **応答本文のログを採らない** — 結果データの保存は情報資産を増やし「そちらにデータは残るのか」と問われる材料を自ら作るため、既定は無効。(3) **野良AI排除は不要** — RepChatにはAIが1つしかない。**既知の穴**: レポート定義の履歴はGitが担うが**権限変更の履歴が無い**（内部統制監査で問われる）。ADR-0010 D7より権限をGitに置くのは不適で、DB＋変更履歴テーブルが見立て。**全項目に着手条件を付し、顧客がいない段階で作り始めないことを明記**（この製品が一度通った失敗の反復を防ぐため） | [docs/ai-governance-requirements.md](../docs/ai-governance-requirements.md) |
| 2026-07-27 | LOG-0060 | **ADR-0012 T7（環境を消すのも一発）を実測で確認**し、あわせて検証ハーネスの欠陥を1件修正。`make destroy ALLOW_DESTROY=yes` で**13破棄**、`make deploy` で**13再作成**、Cloud RunのURLは同一（ホスト名のハッシュ部はプロジェクト＋リージョン由来で安定）、bootstrap所有物（4シークレット・レジストリ・stateバケット）は保持。**「消えること」だけでなく「戻ること」まで確認**した。ここで出た2つの事実。**(1) SAを作り直すと外部のIAM付与は引き継がれない**: 同名で再作成しても `unique_id` が変わるため、旧SAを指すバインディングは `deleted:serviceAccount:...?uid=<旧unique_id>` として残り新SAには適用されない（再付与時の出力の uid が destroy ログの旧 `unique_id` と一致することで確認）。実テナント分は `tenants.tf` が同じ apply で復元するので影響は無く、**Terraform管理外の手動付与だけが取り残される**（今回はLOG-0052の手動フィクスチャが該当）。**(2) Cloudflare KV はGCPの破棄を生き延びる、そしてそれが検証を騙す**: 環境を作り直した直後のライブE2Eが10/10で通ったが、監査ログに `query.execute` が1行も無く、**②経路は一度も実行されずKVキャッシュから返っていた**。アサーション8は「executor → BigQuery」を名乗りながら、キャッシュが温まっていれば**何も検証せずPASSする**。`data_version` を上げてキャッシュを冷やしてから再実行し、初めて②の復旧を実証（監査行にテナント別SA・自データセット・行スコープ述語を確認）。**修正**: `verify.mjs` が起動時に自らデモテナントの `data_version` を上げ、アサーション8に `cached === false` を要求する。**`make deploy` には入れない** — `data_version` は「テナントのデータが更新された」という業務的事実であり、リリース毎に上げると全テナントの結果キャッシュを毎回無効化することになり意味論もコストも誤り | [spikes/live-e2e/verify.mjs](../spikes/live-e2e/verify.mjs) |
| 2026-07-27 | LOG-0059 | **ライブE2E 10/10 — デプロイ済み構成を実HTTP・実JWTで貫通**（Cloudflare Workers → Cloud Run → Neon → BigQuery、スタブ無し）。①シェル・認可否認・②データ・キャッシュ・**越境ゼロ**まで全通過。証拠はBigQueryのジョブ履歴で、`t-alpha-reader@` が `... FROM (SELECT * FROM t_alpha.orders WHERE store_id IN ('s1'))`、`t-bravo-reader@` が `t_bravo.orders WHERE store_id IN ('s9')` を**それぞれ自分のSAとして**実行している。ADR-0010 D1（接続主体はテナント別）とバインダの行スコープが、本番構成で同時に成立していることの実測。**より重要なのは、ここで初めて出た3つの欠陥がいずれもローカル検証では原理的に発見不可能だったこと**。**(1) Workersでの `fetch` 剥がし**: `this.#fetch = globalThis.fetch` と保持して `this.#fetch(...)` で呼ぶとレシーバがアダプタになり、Workersは `Illegal invocation` で拒否する。gateの両HTTPアダプタが該当し、**リクエストがアイソレートを出る前に**全滅していた。既存テストが全て `fetchImpl` を注入していたため本番既定パスが一度も実行されておらず、緑のまま素通りしていた（回帰テストを追加）。**(2) Cloud Runメタデータトークンのスコープ厳密性**: なりすましの source に `AdcTokenProvider` の既定（BigQueryスコープのみ）を渡していたため、IAM Credentials が **403**。`tokenCreator` の付与・API有効化・実行SA、すべて正しくても通らない。source には `cloud-platform` が要る（なりすまし後のトークンはbigqueryのまま＝テナント側の権限は不変）。**LOG-0052のローカル実測が通っていたのは、ローカルADCが `cloud-platform` を持つユーザー資格情報だから**で、アタッチされたSAとして動かして初めて出る差。**(3) 観測性**: gateのcatchが例外を握り潰しており、Cloud Runのリクエストログが空という間接証拠からしか推論できなかった。原因タグのログ出力を追加（クライアント応答は不変）。**運用上の最短路**: executorの失敗理由は監査行（`query.refused` / `query.failed` の `reason`）に完全な形で残る。手前の拒否が奥の失敗を覆う（`table-not-allowed` が403を隠していた）ので順に潰す | [spikes/live-e2e/](../spikes/live-e2e/README.md) |
| 2026-07-26 | LOG-0058 | **初回デプロイ完了、ライブ検証5/5**。ADR-0012の一発デプロイが実環境で通り、control-plane / executor の2サービスがCloud Run（asia-southeast1）で稼働。実測: 両サービスの `GET /health` → **200**（＝コンテナ起動かつSecret Managerからシークレット取得成功。`requireEnv` が1つでも欠ければexit 2で起動失敗するため、200は設定が揃っている証拠）、トークン無しの `POST /v1/control` と `/v1/query` → **401**、誤トークン → **401**。**公開状態でも認証境界が実際に閉じていることをライブで確認**。**T4の前提条件が判明**（オーナーの指摘で訂正）: `iam.allowedPolicyMemberDomains` は制約単体の既定こそ「すべて許可」だが、**近年作成された組織にはsecure-by-defaultで自組織限定が自動適用される**ため、T4（公開Cloud Run）は「無料の既定」ではなく**組織ポリシーの明示的除外という追加のセキュリティ判断を要する**。ADR-0012に前提条件として追記。オーナーは選択肢A（プロジェクト単位で親ポリシーを置換、許可＝すべて）を選択し、**組織の既定を1プロジェクト分ゆるめた上で成立している構成**であることを記録する。含意: この組織既定は、LOG-0053で「WIFの唯一の実質的利点」とした**ネットワーク層での拒否**を組織が既に要求しているということでもあり、**WIF案（B）の相対評価はLOG-0053時点より上がっている**（本番運用に向けて再検討の価値あり）。残タスク: gateのCloudflareデプロイ（`CONTROL_PLANE_URL`/`EXECUTOR_URL` と共有シークレットの設定） | [ADR-0012](../docs/adr/0012-terraform-cloud-run-deployment.md) |
| 2026-07-26 | LOG-0057 | 初回デプロイの第4の壁で、これは**設計上の制約の発見**。13リソース中11（Cloud Run 2サービス含む）まで作成され、`allUsers` の invoker バインディング2件が `Error 400: One or more users named in the policy do not belong to a permitted customer` で失敗。原因は組織ポリシー **`constraints/iam.allowedPolicyMemberDomains`（ドメイン制限共有）** で、Cloud Runの「未認証を許可」はIAM上 `allUsers` として実装されるため、**ADR-0012 T4（公開Cloud Run＋共有シークレット認証）はこの組織では適用できない**。設計時に組織ポリシーを確認していなかった私の見落とし。失敗は安全側（公開されていない）。対応: `allow_public_invoke` 変数を追加し（既定true）、`false` でinvokerバインディングを作らずapplyを完了できるようにした — ただし**falseでは鍵無しIAM認証手段が無いためゲートから到達できず（LOG-0053）、applyを完了させて残りを検証するための状態であって本番構成ではない**ことを変数の説明とdeploy.mdに明記。恒久対応は3択（A: プロジェクトをポリシー除外＝組織統制を1プロジェクト分ゆるめる／B: WIF自前OIDC発行者＝組織ポリシーと整合するが実装量大／C: Cloudflare Tunnel等の前段）で、**いずれもセキュリティ判断なのでオーナーに委ねる**。重要な含意: **この組織ポリシーは、LOG-0053で「WIFの唯一の利点」と評価した"ネットワーク層での拒否"を組織が既に要求しているということ**で、Bの相対評価は当時より上がっている。あわせて**別のバグを修正**: `google_cloud_run_v2_service` は `deletion_protection` の既定がtrueで、**課金対象リソースそのもので `make destroy` が失敗する**（ADR-0012の「一発で消せる」要件を壊す）ため明示的にfalseを設定 | [docs/deploy.md](../docs/deploy.md) |
| 2026-07-26 | LOG-0056 | 初回デプロイの第3の壁（LOG-0055に続く）。イメージのビルドとpushは成功したが `terraform init` が `auth: "invalid_grant" "reauth related error (invalid_rapt)"` で失敗。原因は**Terraformがgcloud CLIのセッションではなくApplication Default Credentials (ADC) で認証する**こと。両者は独立に期限切れするため、bootstrapの全gcloud操作が成功した直後でもADCだけ失効していることがある（Workspaceドメインの再認証ポリシー下では起きやすく、同じ実行中にgcloud側だけ再認証されていた）。解決は `gcloud auth application-default login` ＋ `set-quota-project`（後者は再ログインでリセットされることがあり、LOG-0052でD1スパイクの403を引き起こしたのと同じ設定）。**ハードニング**: `deploy.sh`/`plan.sh` の先頭でADCがトークンを発行できるか検証し、駄目なら復旧コマンドを表示して即座に停止する。これが無いと、1分かけてイメージをビルド・pushした**後で**不透明なバックエンドエラーとして現れる（fail fast、COD-011） | [infra/scripts/deploy.sh](../infra/scripts/deploy.sh) |
| 2026-07-26 | LOG-0055 | 初回の実デプロイで判明した2つのGCP運用事実（`make deploy` 実行、bootstrapは全成功しビルド段で失敗）。**(1) API有効化の伝播遅延**: `cloudbuild.googleapis.com` を有効化した数十秒後の `gcloud builds submit` が `PERMISSION_DENIED` を返す。他のAPI（Artifact Registry・GCS・Secret Manager）は成功したのに Cloud Build だけ失敗したのが切り分けの決め手で、**単純な再実行で解消**（`make deploy` は冪等に作ってあるため作成済みは skip される）。**(2) Cloud Build のデフォルトSAが権限ゼロ**: Cloud Build は Compute Engine デフォルトSA（`<PROJECT_NUMBER>-compute@developer.gserviceaccount.com`）で動くが、**新しいプロジェクトではこのSAへの Editor 自動付与が廃止**されたため権限ゼロで、渡されたソースtarballすら `storage.objects.get` できず `INVALID_ARGUMENT: could not resolve source ... forbidden` になる。自動付与の廃止自体は正しい（デフォルトIDへの常時Editorは避けるべき常時特権）ので、bootstrapで **`roles/cloudbuild.builds.builder` を明示付与**する修正を入れた（ソース読取・ログ書込・イメージpushを含む目的別ロール。個別permissionを手で組むと取りこぼして同じ学習を繰り返すため単一ロールを選択）。Compute Engine APIは有効化しない（SAは既に存在し、使わないAPIを増やさない） | [infra/scripts/bootstrap.sh](../infra/scripts/bootstrap.sh) |
| 2026-07-26 | LOG-0054 | ADR-0012 の実装。`make deploy` / `make destroy ALLOW_DESTROY=yes` / `make infra-plan` を追加（canonical契約の下のproject-specificターゲット、`destroy` は profiles rule 3 / GR-031 に従い明示オプトイン＋対話時はプロジェクトID再入力＋help に DANGEROUS 表示）。構成: `Dockerfile`（1イメージ・Node 24のネイティブ型ストリップでビルド工程なし・非rootユーザー）、`infra/scripts/bootstrap.sh`（API有効化・Artifact Registry・GCS stateバケット・**シークレット投入**）、`infra/terraform/`（実行SA×2・最小権限のsecretAccessor・Cloud Run×2＝同一イメージのコマンド差し替え・invoker=allUsers・テナント3点セット）。**秘密はTerraform stateに入らない**（T3）— bootstrapが `.env` から gcloud で Secret Manager に直接入れ、TFは名前を参照して権限を付けるだけ。値はログにもargvにも出さず、既定では既存シークレットを上書きしない（`ROTATE_SECRETS=yes` で明示ローテ）。APIは意図的にTF管理外（destroyで無効化されると破壊的なため）。テナントのデータセットREADERは `google_bigquery_dataset_access`（LOG-0052でallowlist要件を実測したsetIamPolicy経路を回避）。**リージョン既定 `asia-southeast1`**（Neonがsingapore固定のため。gate→CRは1往復だがCR→Neonはトランザクションで複数往復＝おしゃべりな脚を短くする。**推定であり実測ではない**ので、deploy.md §6 に計測項目として明記し、違えば変数を張り替える＝Cloud Runはステートレスなので `apply` のみ）。`min_instances` 既定0（無トラフィック時の課金ゼロ優先、SLA約束時に1へ）。**`#authenticate` の `getTenantEpoch`+`getUser` 統合はCOD-051により保留**し、deploy.md §6 の計測項目として記録（実測でボトルネックと確認できてから着手＝オーナー合意）。マイグレーションは `make deploy` に含めない（T1: インフラ構築とデータDDLは失敗ドメインが違う）。`terraform fmt -check` / `validate` 通過 | [ADR-0012](../docs/adr/0012-terraform-cloud-run-deployment.md) |
| 2026-07-26 | LOG-0053 | ADR-0012 T4 の理由を訂正（判断自体は維持）。オーナーの指摘「CloudflareにSAキーを貼っているのか／WIFは使えないのか」を受けた調査結果。**初版の理由「IAM認証にはSA鍵をWorkersに置く必要がある」は誤り** — WIFはまさに鍵を避ける仕組み。一次情報を当たった結論: **Workersには鍵無しでGCP IAM認証を行う手段が現時点で無い**（Cloudflare発行のOIDCトークンはCloudflare Access＝人間のSSO向けで、Worker自身のワークロードIDではない。GitHub Actionsの `ACTIONS_ID_TOKEN_REQUEST_URL` やメタデータサーバ相当が無く、wranglerデプロイのOIDC対応すら議論段階）。よってWIFには自前OIDC発行者が要り、**共有シークレット1つが署名鍵1つに入れ替わるだけで鍵は消えない**。決定的なのは**現行案がCloudflareにGCP資格情報を1つも持たない**ことで、比較は「鍵ゼロ vs 鍵1つ」。爆発半径は 共有シークレット（自社エンドポイントを叩けるだけ・GCP権限ゼロ）< WIF署名鍵（短命・属性条件付き）< **SA鍵（長期資格情報。executorのSAはテナントSAをなりすませるため漏洩＝全テナント露出）**。オーナーが「どうせ鍵が要るならSAキーで良い」と述べたのに対し前提の誤り（現状は鍵ゼロ）を指摘し、**A案＝共有シークレット維持**で合意。再検討の引き金は「Cloudflareがプラットフォーム発行のワークロードIDを提供したら」と明記。実機スパイクは見送り（自前OIDC発行者を作って「やはり鍵が要る」と分かるだけで結論が事前に見えるため）。あわせてADR-0012の「テナントSAはTerraform管理外」という記述を訂正 — **本番テナントSAはTerraformが所有**（SA＋データセットREADER＋executor実行SAへのtokenCreatorの3点セット）。`tenants` 既定 `[]` は「まだ宣言していない」の意味で、LOG-0052の検体2つのみ管理外（テスト検体を本番stateに入れない分離） | [ADR-0012](../docs/adr/0012-terraform-cloud-run-deployment.md) |
| 2026-07-25 | ADR-0012 (proposed) | デプロイを `make deploy` / `make destroy` の一発化。**T1** IaCはTerraformでGCPリソースのみ（Cloudflare=wrangler、NeonのDDL=migrations は管理外＝インフラ構築とデータ変更は失敗ドメインが違うので混ぜない）、**T2** 1イメージ2サービス（Cloud Run側でコマンド差し替え）・ビルドはCloud Buildでローカルdocker不要、**T3** **秘密はTerraform stateに入れない**（Secret Managerの本体とバージョンは冪等bootstrapが `.env` から gcloud で作成、Terraformは参照とアクセス権付与のみ。`secret_version` に値を渡すと平文がstateに残るため）、**T4** Cloud Runは公開＋共有シークレット認証（呼び出し元Workersにgate用のSA鍵を置く方がGR-001/D1と衝突するため、鍵配布より秘密の在り処が少ない方を選ぶ。定数時間比較・fail-closed検証・①境界のサーバ側解決・D1のIAMバックストップが補償的制御。GR-030に照らし姿勢低下ではなくトレードと判断）、**T5** データセット権限は `google_bigquery_dataset_access`（標準ACL）＝LOG-0052でallowlist要件を実測した経路を避ける、**T6** stateはGCSバックエンド（ローカルstate紛失＝孤児リソース＝課金が止められない、「消せること」は機能要件）、**T7** `destroy` は profiles rule 3 / GR-031 に従い `ALLOW_DESTROY=yes` の明示オプトイン＋help に DANGEROUS 表示。既存のテナントSA（LOG-0052で手動作成）は `tenants` 変数の既定 `[]` で管理外に置き、黙って作り直して権限を壊さない | [ADR-0012](../docs/adr/0012-terraform-cloud-run-deployment.md) |
| 2026-07-24 | LOG-0052 | ADR-0010 D1 バックストップを**実BigQueryでライブ検証**（`spikes/executor-d1-backstop/verify.mjs`、オーナー実行、5/5）。LOG-0033 が②バインダ出力の証明だったのに対し、これは**独立した③データ層**の証明: バインダを完全にバイパスして越境SQL（`SELECT ... FROM t_bravo.orders` を alphaのSAでなりすまし実行）を直接ランナーに渡しても、BigQuery が `Access Denied: User does not have permission to query table` を返す＝AST書き換えではなくIAMが止める。実測: alpha SA→t_alpha 読める(n=5)/bravo SA→t_bravo 読める(n=3)、各SAは相手データセットを拒否される、実行時自身IDは両方読める(n=3、対比＝なぜなりすましが要るか)。`ImpersonatingTokenProvider` が IAM Credentials `generateAccessToken` で短命トークンを都度発行（鍵不保存、GR-001）。あわせて migration 004 を実Neonへ適用（RLS 6/6）、control-plane-neon スパイクで resolver が `datasources.project_id`/`connection_ref` から D1 identity を返すことを実Neonで確認（14/14、LOG-0047の実証）。セットアップの実運用知見: (a) `bq add-iam-policy-binding` は環境によって "requires allowlisting" で失敗する→データセット標準ACL（`bq update` の access配列に READER 追加）が確実、(b) `generateAccessToken` の 403 は IAM Service Account Credentials API 未有効＋ADC quota project ズレが原因→`iamcredentials.googleapis.com` 有効化＋`gcloud auth application-default set-quota-project` で解消。これで status.md §2.3 の「BigQuery側③は未成立」を実証済みへ更新。残るは実デプロイのみ | [spikes/executor-d1-backstop](../spikes/executor-d1-backstop/README.md) |
| 2026-07-24 | LOG-0051 | デプロイrunbook（PR-2、docs/deploy.md）。4デプロイ対象（gate=Workers、control-plane/executor=Nodeサービス、Neon=マネージド、BigQuery）のトポロジ（mermaid）、サービス別の設定表（必須/秘密/既定を明記）、順序付きデプロイ手順（Neonプロビジョン→秘密生成→migrate→control-plane→executor→gate→スモーク）、ローカル開発、シークレット運用（GR-001: リポジトリに置かない、`wrangler secret`/Secret Manager、共有秘密はgate側とサービス側を同時ローテ、connection_refは非機密）。`wrangler.toml` にcontrol-plane用 vars コメントを追加（executor と同型）＋runbookへのポインタ。変数名のみ記載（値は書かない）。status.md索引に deploy.md を追加。これで「(a)合成ルート＋runbook」完了 — 以降はコードではなく実デプロイとオーナー作業（SA作成・ライブ検証・デザインパートナー） | [docs/deploy.md](../docs/deploy.md) |
| 2026-07-24 | LOG-0050 | デプロイ向けの合成ルート（PR-1）。Workers以外の2サービス（control-plane / executor）のNodeエントリを `src/main/` に新設。`serve.ts`＝依存ゼロの `node:http`→Web-Fetch ブリッジ（ハンドラ群はWorkers/Node共通のRequest→Response契約で書かれているため、Nodeではこの薄い橋渡しだけでよい。フレームワーク依存を足さない＝COD-040）。ループバック実ソケットで往復・throw→500 をテスト。`control-plane-server.ts`＝`ControlPlaneDb`＋`PgControlPlaneReader`＋`PgAuditSink`＋`createControlPlaneHandler` を合成。`executor-server.ts`＝`PgBindingResolver`（binding兼catalog）＋`BigQueryRunner{tokens: ImpersonatingTokenProvider{source: AdcTokenProvider}}`＋`PgAuditSink`＋`createExecutorHandler` を合成（＝D1 impersonation をここで実結線）。`env.ts`＝必須変数の欠落は起動失敗（exit 2）＝黙ったデフォルトにしない、値はログに出さない（GR-001）。`QUERY_POLICY`(JSON)は未設定/不正なら空＝全クエリ拒否のfail-closed（LOG-0039、Phase1は注入）。合成ルートは実DB/BQが要るのでCIでは実行不能だが、**起動スモークテスト**（必須env無しでspawn→exit 2＋メッセージ）でimport健全性とfail-closedを担保。npm scripts `serve:control-plane`/`serve:executor` 追加。156/156 green。残: runbook（docs/deploy.md、PR-2）＋実デプロイ | [#65](https://github.com/Yukihide-Mitsuoka/repchat/issues/65) |
| 2026-07-24 | LOG-0049 | コントロールプレーンSEAMを `worker.ts` で結線（PR-B、#83の最後の一片）。`controlPlaneFor(env)` が `CONTROL_PLANE_URL`/`CONTROL_PLANE_TOKEN` の両方が設定されていれば `HttpControlPlane`（PR #99）を、無ければインメモリfixture＋no-op監査を返す（`executorFor` と同型＝誤設定デプロイはfixtureを黙って配信せず明示的に分かる）。`HttpControlPlane` は `ControlPlaneReader` と `AuditSink` の両方を実装するので**1インスタンスで読み取りSEAMと監査SEAMを兼ねる**（HTTP経由なら監査もPostgresへ、fixtureならno-op）。`GateOverrides` に `controlPlane`/`audit` を追加＝Nodeの合成ルートがインプロセスPgアダプタを直接注入でき、env選択より優先（executor override と対称）。ワーカーテスト追加: global fetch を実ハンドラに向け、fixtureに無い `r_real`(v9) が解決することで `HttpControlPlane` 結線を実証。150/150 green。**これで #83（コントロールプレーン実装）はコードとして完了** — 残るのはコントロールプレーン・サービスのNodeホスティング（デプロイ、`createControlPlaneHandler`＋Pgアダプタの合成ルート） | [#65](https://github.com/Yukihide-Mitsuoka/repchat/issues/65) |
| 2026-07-24 | LOG-0048 | Workers対応のコントロールプレーン・トランスポート（#65のexecutor版と同型、PR-A）。Workersゲートは `PgControlPlaneReader`/`PgAuditSink` を直接使えない（porsagerドライバはNode専用のTCPソケット）ため、executorと同じくHTTP越しに到達する。Node側 `control-plane/interface/http.ts` の `createControlPlaneHandler`（`POST /v1/control`、op=tenantEpoch/user/reportVersion/dataVersion/audit、共有シークレットを定数時間比較で検証）＋ゲート側 `gate/infrastructure/http-control-plane.ts` の `HttpControlPlane`（`ControlPlaneReader`+`AuditSink` を実装）。**fail-closed設計の要点**: 読み取りはトランスポート/非200で**throw**し、`null` は成功時の「該当なし」専用に留める（＝到達不能を「unknown-principal」に潰さない）。これを受け `handler.ts` に try/catch を追加し、任意のアダプタ例外を汎用500へ（スタックトレース非漏洩・ハング防止）。監査書き込みは**例外を握りつぶす**（配信済みリクエストを監査失敗で落とさない＝no-op sinkと同契約）。応答のgrantsは防御的に再検証（不正なdataScope kindは致命扱い＝throw、開かない）。トランスポートの信頼方向はexecutorより狭い: ゲートは「尋ねる」だけでテナント境界を主張せず、RLSがアダプタ内で app.tenant_id から解決する。ラウンドトリップ・テスト（クライアントfetch→実ハンドラ）で両端の線契約を検証。149/149 green。残: worker.ts の結線（controlPlaneFor/auditFor、PR-B） | [#65](https://github.com/Yukihide-Mitsuoka/repchat/issues/65) |
| 2026-07-24 | LOG-0047 | ADR-0010 D1 実装の第3歩（PR-3、実行時結線）。migration `004_datasource_d1_identity.sql`: `datasources` に `project_id text not null` を追加（3段階の安全な手順＝add nullable→backfill→set not null。既存dev行の有無に依らず適用可、新規行はproject_id必須＝D6）、`connection_ref` を nullable 化し**意味を変更**（旧: Secret Manager参照 → 新: D1 credentialRef＝なりすまし対象のSAメール、NULL＝実行時自身のID）。インパーソネーション（PR #95）では保存する秘密が無いため、connection_ref は非機密の識別子になる。`PgBindingResolver` は注入していた `hostProjectId` を撤去し、`dataset`/`project_id`/`connection_ref` を行から読んで完全な `QueryIdentity` を返す（原則E: 呼び出し側からは一切受け取らない）。app_runtime のSELECT付与はテーブル単位なので新列も自動被覆。control-plane-neon スパイクに identity 検証を追加（seedにproject_id＋SAメール形connection_ref、resolveがそれらを返すことを確認）。system-design.md のdatasources DDL/ER/秘密情報ノートを impersonation の実態に合わせて訂正（DOC-040: 旧「Secret Manager参照のみ」はコードと矛盾）。140/140 green。**残るデプロイ作業**: 実executorサービスの合成ルートで `ImpersonatingTokenProvider` を挿すこと（未デプロイ）と、オーナーが実SAを作り connection_ref に投入すること | [migrations/004](../migrations/004_datasource_d1_identity.sql) |
| 2026-07-24 | LOG-0046 | オーナーの明示的な要請で、実GCPプロジェクトID（旧 `<gcp-project>`）をリポジトリの作業ツリーから全削除（37箇所/18ファイル）。置換方針: テスト＝リテラル `example-project`、実BigQueryを叩くスパイク＝`process.env.GOOGLE_CLOUD_PROJECT`（未設定なら明示エラー、READMEも `GOOGLE_CLOUD_PROJECT` を要求する形に）、BigQueryを叩かない control-plane-neon スパイク＝`example-project` プレースホルダ、散文（docs/ADR-0006/glossary/system-design/coding-rules）と decision-log 履歴＝`<gcp-project>` に redact（LOG-0038 の前例に倣い、意味は不変・トークンのみ置換）。GCPプロジェクトIDは資格情報ではないが、オーナーのプライバシー要請として尊重。**重要な限界**: これは作業ツリーの scrub であって git 履歴の書き換えではない — 過去コミット（マージ済み #93/#95 等）とGitHub上には残る。履歴からの完全削除は filter-repo 等での履歴書き換え＋force-push が必要で、GR-011（共有ブランチの履歴書き換え禁止）に該当し全クローン/PRを破壊するため、別途オーナーの明示承認が要る（本PRでは実施しない） | [COD-005](coding-rules.md) |
| 2026-07-24 | LOG-0045 | ADR-0010 D1 実装の第2歩（PR-2）。`ImpersonatingTokenProvider` を実装（`AccessTokenProvider` の2つ目のアダプタ＝ARC-005）。実行時は自分のADC IDで認証し、IAM Credentials `generateAccessToken` にfetchで問い合わせてテナント別SAの**短命トークン**を取得する。**鍵ファイルを一切ダウンロード/保存しない**（必要権限は各SAへの `serviceAccountTokenCreator` のみ、GR-001/D6適合）。fail-closed設計: `null` refは素通し（自分のID＝開発/フォールバック、バックストップ無しを明示的に選択）、SAメール形以外のrefは拒否（壊れた制御プレーン値がなりすまし試行にならない）、IAM拒否/非パース/空トークンは全てthrow→BigQueryRunnerが500にマップし監査側に詳細を残す。ユニット8本（fake fetch、ネットワーク無し）。あわせて**ライブ・バックストップ検証スパイク** `spikes/executor-d1-backstop/`（+gcloud runbook）を用意: バインダを完全にバイパスして越境SQLを直接渡し、BigQueryがIAM層で拒否することを実証する設計（オーナーがSA作成後に実行）。dataset-level dataViewer付与は `bq add-iam-policy-binding PROJECT:DATASET` を一次情報で確認済み。実行時有効化（`credentialRef` の結線）とproject_id列はPR-3。あわせてstatus.md §3の②行スコープ構造検証の行が #89 マージ後もstaleだったのを訂正（DOC-040） | [spikes/executor-d1-backstop](../spikes/executor-d1-backstop/README.md) |
| 2026-07-24 | LOG-0044 | ADR-0010 D1 実装の第1歩（PR-1、コードのみ・インフラ不要・挙動保存）。接続主体を「実行時の単一アイデンティティ」から「テナント別に解決される機械アイデンティティ」へ変えるためのシーム（executor内部のContract変更）を敷いた。`QueryIdentity{projectId, credentialRef}` を新設し、`TenantDataset` に両フィールドを**必須**で追加（省略はコンパイルエラー＝共有資格情報への暗黙フォールバックを構造的に禁止。scopeColumn必須化=LOG-0042と同じD6規律）、`QueryRunner.run(sql, params, identity)`、`AccessTokenProvider.getToken(credentialRef)` に変更。identityはdatasetと同じく**サーバ側でtenantIdから解決**しbindには渡さない（原則E）。`AdcTokenProvider` はnull以外のrefをfail-closedで拒否（なりすまさない開発/フォールバック用途と明示）。`BigQueryRunner` はprojectId/tokenをコンストラクタではなく呼び出し毎のidentityから取る。`PgBindingResolver` は `hostProjectId` 注入で `credentialRef: null` を返す（=現行挙動を保存、connection_ref読み出しとimpersonationはPR-2/3）。決定事項2件（オーナー確認済み）: 認証方式は**インパーソネーション**（IAM Credentials generateAccessToken・短命トークン・鍵ファイル不保存、D6/GR-001適合）、<gcp-project> にテスト2テナントのバックストップ検証環境を作る（手順は私が用意しオーナーが実行）。132/132 green。既知の限界: per-tenant SAはGCPのプロジェクト当たりSA数上限(~100)に当たる—初期スケールでは非問題、シャーディングは将来項目。spikes/executor-bigquery/* は scope 必須化前から stale（CI非対象、PR-2のライブスパイクで作り直す） | [ADR-0010](../docs/adr/0010-connection-identity-is-never-a-person.md) |
| 2026-07-24 | ADR-0011 (accepted) | repository owner が PR #90 のマージにより ADR-0011 を承認（owner-merge = 受理、ADR-0009 の前例に倣う）。接続データソースの4階層化、2つ目のソースはD3検証で選ぶ、Parquet第一級／JSON・CSVは取り込み、SaaS APIコネクタは作らない | [ADR-0011](../docs/adr/0011-datasource-scope-and-tiers.md) |
| 2026-07-24 | ADR-0010 (accepted) | repository owner が PR #87/#88 のマージにより ADR-0010（D1–D7）を承認。あわせて実装状況を訂正（status.md §2.3）: 多層防御の③データ層バックストップは**管理データ（Postgres/RLS）では成立、分析データ（BigQuery）では未成立** — 実行時は単一の `AdcTokenProvider` が全テナントのデータセットに到達でき、テナント別SA（D1）は未実装。LOG-0033 の実BQ 7/7 は②バインダ出力の証明であって③の保険ではない、と明記。次の実装はD1（テナント別機械ID） | [ADR-0010](../docs/adr/0010-connection-identity-is-never-a-person.md) |
| 2026-07-24 | ADR-0011 (proposed) | 接続データソースの範囲を階層化する。**S1: ソースに要求する能力は4つだけ**（資格情報のスコープを粗く絞れる／スキーマが安定／境界で取り出せる／**独自の細粒度ACLを持たない**）。4つ目が実質的な選別条件。**S2: 4階層**（1=BigQuery実証済み、2=オブジェクトストレージ、3=Snowflake・RDB読み取りレプリカ、4=SaaSアプリAPIは自前コネクタを作らない）。**S3: 2つ目のソースは市場需要ではなくD3を最も安く検証できるかで選ぶ** — D3はARC-005の意味で未実証であり、オブジェクトストレージはBigQueryから最も遠い（サーバ無し・SQLエンジン無し・IAM行ポリシー無し）ため最小工数で最強の検証になる。Snowflakeを2番目にすると似すぎていて何も検証できない。**S4: Parquet＝第一級のクエリ対象**（列プルーニング・行グループ統計・パーティションプルーニングが効くので「プッシュダウンが無い」は当たらず、エンジンはこちらが持ち込む）、**JSON/CSV＝取り込み**（境界で検証しParquet化。ライブ対象にしない）。**S5: SaaS APIコネクタは作らない**。Confluence型を外す理由は**コストではなくアクセス制御** — 独自のページ単位ACLを無視すれば漏洩、ミラーすればD7が「委ねてはいけない」とした細かく実行時に変わる境界を逆向きに取り込むことになり、D6で開く側に倒れる。安全な集計を事前に区別できない点が決定的（ドリルダウン1段で漏れる） | [ADR-0011](../docs/adr/0011-datasource-scope-and-tiers.md) |
| 2026-07-24 | LOG-0043 | MAツールのエクスポート形式を実地確認し、**見立てが逆だったが結論はむしろ補強された**（ADR-0011 S4）。「AmplitudeがParquetで出力できるので、Parquet対応すればMAカテゴリがついてくる」という想定に対し、Amplitudeの**S3エクスポートはzipされたJSON**（時間単位パーティション、1行1イベント）であり、**Parquetは取り込み側**（S3をソースとするコンバータ設定）に現れる。したがって**Parquetだけを受け付ける設計ではMAカテゴリはついてこない** — ベンダがエクスポートで吐くのはJSON/CSVだから。S4の取り込み経路（境界で検証→Parquet化）は衛生上の配慮ではなく、このカテゴリを覆う実際の仕組みである。より一般には、GA4もAmplitudeもウェアハウス連携を持つ＝**データ志向のソースではウェアハウスが既に統合点**であり、直結コネクタは顧客のスタックが既にやっていることの下手な再実装になる。差別化はコネクタ本数ではなく埋め込みマルチテナントのエンドユーザー単位認可（ADR-0010 D2）にある | [ADR-0011](../docs/adr/0011-datasource-scope-and-tiers.md) |
| 2026-07-24 | LOG-0042 | LOG-0040 のギャップを塞いだ。**②行スコープの自己検証**を実装（ADR-0010で採用済み）。①側の `verifyAllQualified` と対称に、書き換え後のSQLを**2度目の再パース**にかけ、行スコープ対象の表がすべて**その主体自身の**スコープフィルタ（同じ列・同じ店舗集合）の中にあることを検証する。**2つで1組**であることが要点: (a) `TableRule.scopeColumn` を**必須化**し `null` を「意図的に行スコープなし」の明示とした — 省略は「忘れた」と区別できず、忘れると全行返るため `policy-incomplete` で拒否する。自己検証はポリシーを読む側なので**ポリシー自体の誤りは検出できない**（同じ真実源）。(b) 構造検証はバインダ側の取りこぼしを捕捉する。どちらか一方では不十分。検証関数は `assertRowScopeBound` として公開 — 拒否経路は「正しいバインダなら生成しないSQL」を与えないと証明できないため。ミューテーション検査で確認: 折り返し処理を無効化すると bind.test.ts が5件失敗する（＝検証が実際に噛んでいる）。128/128 green。**限界も明記**: これは同一プロセス・同一パーサ上の自己点検であって独立層ではなく、②に真の独立層を与える候補は成果物ベースのみ（鮮度SLA待ち、LOG-0041） | [ADR-0010](../docs/adr/0010-connection-identity-is-never-a-person.md) |
| 2026-07-24 | LOG-0041 | ADR-0010 に D6・D7 を追加。オーナーの評価方針（堅牢性＋／コストと速度の犠牲−、アクセス制御は設計の要）に対し「**保守工数はコストではなく堅牢性に含める**」という補正が入ったことによる。**構築工数（一度きり、度外視可）と保守工数（恒常的、堅牢性の入力）を分ける**のが要点。保守工数が堅牢性に効く経路は3つ: 未追随ドリフト（保守されない制御は「無くなる」のではなく古いまま残る＝セキュリティ制御の典型的な壊れ方は不在ではなく陳腐化）、運用容量が天井（開発者1名。容量を超える層は誤認を生む分だけ無いより悪い）、触るたびの誤り機会。**D6: 採否は「保守の量」ではなく「ドリフト時にどちらへ倒れるか」で決める**（閉じる側なら量が多くても可、開く側なら量が少なくても不可）。**D7: 粗く滅多に変わらない境界（テナント＝商談・オンボーディングに紐づく）はデータ層に委ね、細かく実行時に変わる境界（ロール・スコープ＝顧客管理者が自助で随時変更）は委ねない**。D7は原則Eが既に引いていた線と同一で、原則Eの論拠（実行時カスタムロールをDDLで扱えない＝表現力）に保守ドリフトという独立した2本目の論拠が加わった形。既存判断は変更なし。この補正により、私が一度「工数を度外視すればソース別ネイティブ委譲も復活する」と述べたのは撤回 — 保守を発生させるのはベンダで日程を選べず、ドリフトが開く側へ倒れるため、却下理由が「保守が高い」から「堅牢性が下がる」に変わっただけで結論は元のまま | [ADR-0010](../docs/adr/0010-connection-identity-is-never-a-person.md) |
| 2026-07-24 | ADR-0010 (proposed) | 接続主体は決して人間にしない。ADR-0005 §9.2（BigQuery接続型のテナント別SA）をソース非依存の規則へ一般化する: D1 接続主体はテナント単位の機械アイデンティティで役割は爆発半径の限定のみ、D2 認可主体はエンドユーザー本人で評価は自層の単一点、D3 セキュリティ実装の数をコネクタ数に比例させない（新ソース＝コネクタの追加であってアクセス制御機構の追加ではない）、D4 プッシュダウンは最適化であって制御ではない、D5 顧客指定プリンシパルは既定ではなくテナント単位オプション（②キャッシュ共有を放棄する代償つき）。Looker Studio型のジレンマの原因は「どちらの人間か」ではなく人間が接続主体であること自体にある | [ADR-0010](../docs/adr/0010-connection-identity-is-never-a-person.md) |
| 2026-07-24 | LOG-0040 | ADR-0010 D4 が露出させたギャップ: 原則C-3（ソース層＝最後の砦）は①テナント分離にしか掛かっておらず、**同一テナント内の②行スコープにはバックストップが無い**。`QueryPolicy` の表定義で `scopeColumn` を書き忘れた表は `bind.ts:118` で `rowScoped === false` となり、エラーにならず全行を返す。これは原則E（②をRLSで実装しない — 実行時カスタムロールをDDLで扱えない）の意図的な帰結だが、D4の観点では「SQL書き換えという最適化手段を②の唯一の制御にしている」状態。ソース非依存のバックストップが要る。D6で評価した結論（LOG-0041）: **行スコープの構造検証を無条件で採用**（書き換え後に再パースし、行スコープ対象の表が必ずフィルタ副問合せ内にあることを検証。①側 `verifyAllQualified` の対称形。自己強制でドリフトは閉じる側、保守はむしろ減る）。ただしこれは同一プロセス・同一パーサの自己点検で**独立層ではない**。等価類ごとの機械プリンシパルはD6/D7で却下（顧客管理者の実行時ロールに追随が必要でドリフトが開く側へ倒れ、しかも剥奪すべき瞬間に失敗する）、結果セット検証は集計クエリにスコープ列が無くカバー率不足で却下。よって**②に真の独立層を与えられる候補は成果物ベースだけ**になり、採否は鮮度SLA次第で保留。鮮度が許さない場合は「②の独立層は無い」と認める。あわせて D1 は §9.2 で決定済みだが**未実装**（`AdcTokenProvider` は単一ADC ID、`TenantBinding` に projectId と `datasources.connection_ref` が未接続）、D3 は接続先が1種（`type in ('bigquery')`）のため**主張であって実証ではない**（ARC-005） | [ADR-0010](../docs/adr/0010-connection-identity-is-never-a-person.md) |
| 2026-07-24 | LOG-0039 | Control-plane Postgres adapters shipped (#83 second half), verified 13/13 on live Neon (ap-southeast-1): PgControlPlaneReader (gate's ControlPlaneReader — epoch, user+grants, report/data versions), PgBindingResolver (executor's BindingResolver+QueryCatalog — dataset, table policy, queryId→SQL), PgAuditSink. All tenant-scoped reads run inside ControlPlaneDb.withTenant, which opens a tx and set_config('app.tenant_id') so RLS is the backstop; the connection is app_runtime (RLS-constrained), never the owner. The load-bearing proof: two tenants seeded with the SAME external_subject resolve to different users/grants because the DB, not the query, isolates them. Two Phase-1 scoping calls, both deliberate: (a) the executor QueryPolicy table-allowlist is INJECTED not stored — one datasource shape, so a per-tenant table would be speculative (COD-051), becomes data when a second shape appears; (b) app_runtime is INSERT-only on audit_logs (the app writes the trail, never reads it — audit reads are an owner op), so audit isolation is tested write-side via WITH CHECK. Node-only (porsager driver uses TCP sockets); the Workers gate needs a control-plane transport (follow-up mirroring #65) or the Neon serverless driver. worker.ts SEAM wiring is the remaining piece | [spikes/control-plane-neon](../spikes/control-plane-neon/README.md) |
| 2026-07-24 | LOG-0038 | Product renamed ChatChart → **RepChat**; repo slug renamed chat-chart → repchat (owner decision; old GitHub URLs survive via redirects). Applied as a mechanical repo-wide substitution at the owner's explicit request, INCLUDING the display name inside historical decision-log/ADR prose — those entries' meanings are unchanged, and the historical identifier `chatchart_app` (LOG-0030's record of what the DB role was called before it was neutralized) is preserved verbatim so history stays truthful. COD-005 paid off exactly as designed: zero code identifiers, DB roles, dataset prefixes, env vars or config keys contained the name, so the entire rename is prose plus two repo-slug URLs (`.github/ISSUE_TEMPLATE/config.yml`, `template-sync.yml` comment) and CHANGELOG links. Name-independent IDs stay: GCP project `<gcp-project>`, DB role `app_runtime`, dataset prefix `t_`, worker name `gate`, package name `app`. COD-005/glossary amended: the repo slug is now the one deliberate exception that follows the product name, because GitHub redirects make that rename cheap — an escape hatch code identifiers don't have | [COD-005](coding-rules.md) |
| 2026-07-22 | ADR-0009 (accepted) | repository ownerがPR #73のマージにより、`ai-dev-foundation` を直接親に維持したまま、テナントスコープ付きデータアクセス、固定の権限語彙、境界入力検証、安全なエラー変換だけをRepChatへ採用する決定を承認した | [ADR-0009](../docs/adr/0009-adopt-portable-saas-design-constraints.md) |
| 2026-07-22 | ADR-0009 (proposed) | 継承元は `ai-dev-foundation` のまま維持し、`nextjs-saas-template` からテナントスコープ付きデータアクセス、固定の権限語彙、境界入力検証、安全なエラー変換という移植可能な制約だけを採用する。Next.js、Clerk、Stripe、Prisma、Cloud Run固有の実装は採用しない | [ADR-0009](../docs/adr/0009-adopt-portable-saas-design-constraints.md) |
| 2026-07-22 | LOG-0037 | 直接親lockを `86eb924` から次のfirst-parentコミット `01eb97c`（ai-dev-foundation #57）へ進め、Rulesetのみで保護されたbranchの旧Branch Protection 404を管理権限確認時だけ「不存在」と判定する修正・回帰テスト・runbookを正確に取り込む。権限・通信の曖昧さは引き続きfail-closed `unknown` とする | [Issue #56](https://github.com/Yukihide-Mitsuoka/ai-dev-foundation/issues/56) |
| 2026-07-20 | LOG-0036 | Gate→executor HTTP transport shipped (#65), completing the production Workers topology left open by #55: `executor/interface/http.ts` (`POST /v1/query`) plus `gate/infrastructure/http-executor.ts`, both fetch-based so the client runs on Workers where the in-process executor cannot (BigQuery credentials come from google-auth-library, Node-only). Moving to HTTP creates a new trust boundary — the caller now asserts tenantId and row scope over the wire — so the service authenticates the calling gate with a shared secret compared in CONSTANT TIME before believing either, and refuses before binding anything. The ① tenant boundary is unaffected: the dataset is still resolved server-side from tenantId and is never sent by the caller, so an authenticated-but-buggy gate still cannot name a dataset (原則E preserved across the wire). Wire input is validated fail-closed: a missing/malformed scope is a 400 and never coerces to all-rows; non-scalar params are refused. Error mapping keeps operator problems away from end users — a 401 (our token is wrong) surfaces as 500, and transport failures return an opaque `executor-unreachable` rather than leaking internal hostnames. `worker.ts` selects the HTTP client when EXECUTOR_URL/EXECUTOR_TOKEN are set and otherwise keeps the in-memory stand-in, so a misconfigured deploy is obvious rather than silently serving fixture data. `QueryCatalog` consolidated into executor/application/ports.ts so both topologies share one definition | [#65](https://github.com/Yukihide-Mitsuoka/repchat/issues/65) |
| 2026-07-20 | LOG-0035 | Gate↔executor seam wired (#55 A-3 complete): `ExecutorQueryAdapter` in gate/infrastructure satisfies the gate's `QueryExecutor` port by delegating to the executor module's `ExecuteQuery`, with a `QueryCatalog` port resolving reportId→SQL. Verified on the full vertical slice against live BigQuery — vendor-signed JWT → gate authz → AST tenant binding → BigQuery → ② KV cache, 8/8, with a single `query.execute` audit entry across two identical requests (the second served from cache without touching BigQuery). Wiring surfaced and fixed a correctness trap: BOTH sides derived the row scope (gate for `scope_hash` in the cache key, executor via `BindingResolver`), so a disagreement would cache rows under a key claiming different visibility. Resolved along 原則E — `BindingResolver` now returns `TenantDataset` (① boundary, infrastructure facts only, never accepted from the caller) while the ② row scope became a REQUIRED argument to `ExecuteQuery.execute`, supplied by the gate which owns authorization, so it cannot default open and has one source of truth. `buildGate` now takes an optional injected `QueryExecutor`: Node composition roots pass the real one, while the Workers entry keeps its fallback because ADC is Node-only and the production topology is gate→HTTP→executor service (ADR-0005 §7) | [spikes/gate-executor-slice](../spikes/gate-executor-slice/README.md) |
| 2026-07-20 | LOG-0034 | Connected-warehouse (customer-owned BigQuery) authentication = **one service account per customer**, which the customer grants `roles/bigquery.dataViewer` on their dataset; the credential reference lives in the control plane as a Secret Manager reference (GR-001). Granularity is the load-bearing part: a single SA shared across customers would make the AST binding the *only* barrier (原則C-3 nullified for this model), whereas a per-customer SA means IAM itself blocks reach — even a total binder failure cannot touch another customer's data, restoring the defence-in-depth that RLS/per-tenant datasets provide in the hosted model. Rejected OAuth-as-end-user: embedded users are the vendor's SaaS users without Google identities; per-user credentials collapse the `scope_hash` equivalence-class sharing the ② cache depends on (99.89% hit rate, LOG-0031); it forces consent/refresh flows inside an iframe, pushes BQ IAM upkeep for every end user onto the customer, and makes Phase-2 custom roles unmappable to IAM principals. Note the accepted trade-off: with an SA the warehouse cannot enforce per-user policies (`SESSION_USER()` resolves to the SA), so within-tenant control stays app-layer — which is where 原則E already put it, and is measured (LOG-0033: same tenant, same SQL, 157,900 all-stores vs 65,000 store-scoped). Hosted-vs-connected remains config, not code: the executor already takes "whose warehouse, whose credential" as a BindingResolver input | [ADR-0005 §9.2](../docs/adr/0005-cache-and-authorization-architecture.md) |
| 2026-07-20 | LOG-0033 | Executor tenant isolation verified end-to-end on live BigQuery, closing PR #59's "NOT YET VERIFIED" caveat: the real pipeline (ExecuteQuery → bindQuery AST rewrite → BigQueryRunner REST + ADC) against two owner-approved per-tenant datasets in `<gcp-project>` scored 7/7 — identical SQL from t_alpha vs t_bravo returns each tenant's own totals from its own dataset, `SELECT * FROM t_bravo.orders` by the alpha tenant is refused pre-execution, row scope and named params work on live data. Makes ADR-0005 §10-6 (per-tenant datasets) concrete for the analytics side. Open follow-up captured (§10-7): this spike uses ADC-as-developer against data WE host; querying a CUSTOMER-owned warehouse instead needs a RepChat service account granted `bigquery.dataViewer` on their dataset, with the credential reference in the control plane (Secret Manager ref, GR-001) — the executor already treats "whose warehouse, whose credential" as a BindingResolver input, so hosted-vs-connected is config, not a code change | [spikes/executor-bigquery](../spikes/executor-bigquery/README.md) |
| 2026-07-18 | LOG-0032 | Postgres RLS tenant-isolation backstop (ADR-0005 §3) proven empirically on real Postgres 16, not just asserted: a representative control-plane subset (tenants/users/roles/user_roles, composite `(tenant_id,id)` FKs) with `enable`+`force` RLS and `USING`+`WITH CHECK` policies, exercised as the non-superuser `app_runtime` role. 7/7 checks pass — a bare `SELECT` with NO WHERE clause returns only the session tenant's rows (the "app forgot the filter" failure mode is contained), unset `app.tenant_id` is fail-closed (0 rows, not all), cross-tenant read-by-id and `WITH CHECK` cross-tenant write are blocked, and the composite FK makes a cross-tenant role grant unrepresentable. Key gotchas recorded: `FORCE` is required (owner bypasses RLS otherwise) so the app must connect as a non-owner role; `WITH CHECK` (not just `USING`) is required to block writes. Not the production migration (that ships with the control-plane module against Neon; Neon runs stock Postgres so behavior is identical) | [spikes/rls-isolation](../spikes/rls-isolation/README.md) |
| 2026-07-18 | ADR-0008 (accepted) | repository ownerがPR #34のマージにより、既定tokenをreadのまま維持し、保護対象・必須CI・no-auto-mergeを補償統制として、ActionsのPR作成設定と定期Template Syncを一体で有効化する決定を承認した | [ADR-0008](../docs/adr/0008-enable-recurring-protected-template-sync.md) |
| 2026-07-18 | ADR-0008 (proposed) | Workflow・`.gitignore`・プロジェクト文書の利用先所有と必須CIを補償統制として、ActionsのPR作成設定と定期Template Syncを一体で有効化する。既定tokenはread、no-auto-merge、追加credentialなしを維持する | [ADR-0008](../docs/adr/0008-enable-recurring-protected-template-sync.md) |
| 2026-07-18 | ADR-0007 (accepted) | materialize未実装期間の一時ブリッジとして、Workflowを利用先所有にした旧Template Syncを手動で一度だけ実行し、変数は実行直後に再無効化する。高権限tokenは追加しない | [ADR-0007](../docs/adr/0007-use-one-time-protected-legacy-template-sync.md) |
| 2026-07-18 | ADR-0006 (proposed) | Edge authorization gate runtime = Cloudflare Workers (+ Workers KV for ②/③/denylist, Cache API for ①): dev on the free tier, production on Workers Paid ($5/mo incl. 10M req + KV + Durable Objects — verified 2026-07). KV's ≤60s eventual consistency equals the ③ authz-cache TTL the design already tolerates; Durable Objects is the named escalation if stricter revocation is ever required. Gate core MUST stay runtime-agnostic pure functions with the Workers APIs isolated in a thin adapter — the in-process spike harness remains the second adapter (ARC-005) and the spike's 12 tests become the acceptance suite. Rejected: Vercel Edge (pricier, KV not first-party, no Next hosting need), Cloud Run min-instances (idle cost > $5 and still needs external KV/CDN parts), always-on self-managed Node (solo-dev ops worst case) | [ADR-0006](../docs/adr/0006-edge-gate-runtime-cloudflare-workers.md) |
| 2026-07-18 | LOG-0031 | ADR-0005 §11 thin-vertical-slice spike passed in-process (12/12 tests, no deps, node:crypto ES256 JWT): cross-tenant cache addressing proven unrepresentable (原則B — forged client tenant_id and forged JWT tenant claim both inert), an induced key-derivation bug caught by the payload tenant assert (原則C-4), version-token invalidation fresh-without-purge, same-effective-scope roles sharing one cache entry (§6), epoch+denylist killing an unexpired JWT instantly, single-flight collapsing 10 concurrent misses to 1 execution. Bench: 99.89% hit rate, p95 0.51ms in-process over 20k requests with ETL bumps every 2k. Noted for §10-1: ES256 verify (~0.25ms) is the latency floor either way. Phase 1 can lift these rules onto a real edge runtime using the test list as the acceptance suite | [spikes/vertical-slice](../spikes/vertical-slice/README.md) |
| 2026-07-18 | LOG-0030 | Rename-proof naming: the product name ("RepChat" — owner not fully settled on it) is a display label only, never a technical identifier (new rule COD-005). Prose renames are one grep-replace; identifier renames are migrations. Applied immediately: DB app role `chatchart_app` → `app_runtime`, BigQuery dataset prefix `cc_` → `t_` in the system-design DDL draft. Already-name-independent stable IDs (GCP project `<gcp-project>`, repo slug) stay unchanged; a future repo rename is covered by GitHub redirects | [COD-005](coding-rules.md) |
| 2026-07-17 | LOG-0029 | Role→permission mappings (which custom role holds which API permissions on which app) stay in Postgres as their system of record, not a separate NoSQL store — extends LOG-0026. Considered NoSQL (document-shaped, read-heavy, seemingly consistency-tolerant) and rejected: consistency is asymmetric (a lagged revoke = a permission that should be gone but persists = security incident, and the epoch+denylist revocation guarantee assumes a consistent SoR); FK/enum integrity catches orphaned grants and typo'd permission names; marginal cost in the already-running Postgres is ~nil while a second store adds ops + a cross-store join + integrity loss for a KB-sized dataset. The "fast NoSQL read" instinct is already served by the existing ③ authz cache (short-TTL KV / signed claims) — SoR ≠ serving path. JSONB column gives document ergonomics within Postgres; a dedicated authz engine (OPA/Cerbos/SpiceDB) is a future option only if policy complexity scales | [ADR-0005 §9.1](../docs/adr/0005-cache-and-authorization-architecture.md) |
| 2026-07-17 | LOG-0028 | ADR-0005 revised (still proposed) to reflect LOG-0026/0027: data layer split into two stores (原則D), access control split into two axes (原則E) — hard tenant isolation (app-layer tenant_id injection + datasource-native row/dataset controls) vs configurable role authorization (app-layer MCP logic + role/permission tables in the RDB; RLS is the wrong tool for runtime-created custom roles). Sections touched: 原則C generalized from "DB layer RLS" to "datasource layer", new 原則D/E, §7 sequence, §9 tech selection, §10 open items (+BigQuery tenant-isolation mechanism, +analytics-data provenance). requirements.md §3 diagram annotated to point here | [ADR-0005](../docs/adr/0005-cache-and-authorization-architecture.md) |
| 2026-07-17 | LOG-0027 | Analytics data store (what NL→SQL queries and Evidence renders) = BigQuery as a first-class connectable source: columnar/serverless, fits the SQL-based core, already validated on the thelook real-schema spike (LOG-0023), GCP-native. DB-layer tenant-isolation backstop moves from Postgres RLS to BigQuery's own controls (per-tenant dataset preferred, or row-access policies / authorized views — §10 open). This is distinct from the control-plane store (LOG-0026); the original ADR-0005 conflated the two into one Postgres | [ADR-0005](../docs/adr/0005-cache-and-authorization-architecture.md) |
| 2026-07-17 | LOG-0026 | Control-plane / tenant-management store (tenants, users, roles, permissions, audit log, per-tenant connection config) = PostgreSQL with RLS, chosen from the start over a cheap NoSQL (e.g. Firestore). Rationale: NoSQL→RDB migration cost is not modest-and-deferrable but a rebuild (data model + query engine + security model); Firestore lacks SQL (breaks the NL→SQL core) and RLS (the stated "safer than Looker" differentiator); and serverless Postgres (Neon/Supabase free tier) already scales to zero, so the initial-outlay concern that motivated NoSQL is near-nil. NoSQL/KV keeps its right place: the edge cache layers (§3/§9) | [ADR-0005](../docs/adr/0005-cache-and-authorization-architecture.md) |
| 2026-07-17 | LOG-0025 | Corrects and completes LOG-0024: Evidence *does* prerender query results — as binary Arrow files under `/api/prerendered_queries/<hash>.arrow` (invisible to the text greps that produced LOG-0024's "results are not baked" claim), and the initial render reads them, so swapping Parquet alone never changes what users first see (the "identical render" was correct behavior, not a preview-tool artifact). Confirmed the corrected model visually: a gate that 404s the prerendered results forces client-side DuckDB-WASM compute over the tenant's Parquet — the same shell build rendered tenant B's chart (3 categories / $39.5k) vs tenant A's (4 / $157.9k), no rebuild. Production options: 404-fallback (simple, slower first paint) or per-tenant `.arrow` generation served under the same hash URLs — the latter maps 1:1 onto ADR-0005's result-cache layer. Evidence's data-caching service worker must be disabled or made tenant-aware | [spikes/evidence-dynamic](../spikes/evidence-dynamic/README.md) |
| 2026-07-17 | LOG-0024 | Evidence dynamic-data integration spike (ADR-0005's shell/data-separation seam): dissected a real Evidence build and confirmed query results are not baked into HTML — only the SQL text is prerendered — while data is fetched at runtime from a content-addressed Parquet URL. Proved, via curl and a genuine in-page fetch() in the actual browser JS realm, that an authorization gate can serve schema-compatible but content-different tenant Parquet through an identical URL with zero rebuild, and that the shell HTML/JS is byte-identical across tenants. Client-side re-render confirmation was inconclusive due to a sandboxed preview-browser tool artifact (flagged as a fast desktop-browser follow-up, not a product risk — this is stock Evidence functionality) | [spikes/evidence-dynamic](../spikes/evidence-dynamic/README.md) |
| 2026-07-17 | LOG-0023 | Real-schema NL→SQL validation on BigQuery `thelook_ecommerce` (schema not designed by us; nullable multi-date/multi-price columns, dialect + qualification requirements): Gemini 3.5 Flash scored 11/12, and the single failure (silently adding an unrequested status filter) was fixed by one general prompt rule → 12/12 at ~¥0.1/question, confirming the prompt-first-then-Opus escalation order of LOG-0022 works in practice. Remaining validation: a design partner's actual schema | [spikes/nl2sql-thelook](../spikes/nl2sql-thelook/README.md) |
| 2026-07-17 | LOG-0022 | NL→SQL model policy: default to Gemini 3.5 Flash on Vertex AI (GCP ADC auth); fall back to Claude Opus 4.8 only when accuracy cannot be recovered by prompt/request changes. Backed by a clean spike run — Gemini 3.5 Flash scored 12/12 on the NL→SQL benchmark (independent-Python verification, incl. ambiguity-clarify and cross-tenant-refuse cases) at ~¥0.1/question, collapsing the earlier "¥200k–500k/month LLM" fear. Caveat: synthetic clean schema; real-customer-schema accuracy still to be validated | [discovery-log §8.1/8.2](../docs/discovery-log.md) |
| 2026-07-16 | LOG-0021 | Business model fixed as "small, reliably profitable" (not hypergrowth): one design partner → few deeply-served customers → side-income business. Cascades: pursue 1 design partner as the sole GTM, treat the 4-month build as effort not a deadline, intentionally cap at 3–5 customers, defer Phase-2 automation (Stripe metered billing, self-serve tenant creation, custom roles) in favor of manual onboarding + flat monthly invoicing, and re-read break-even as "profitable at 3–5 customers." Tenant-isolation security and the NL→SQL core stay non-negotiable | [requirements.md §1.5](../docs/requirements.md) |
| 2026-07-16 | LOG-0020 | Repository owner accepted a temporary exception to ADR-0002 (English-for-docs/) for `docs/adr/0005-cache-and-authorization-architecture.md` and `docs/discovery-log.md`: both stay in Japanese as early-stage product-discovery records; translate before ADR-0005 moves past "proposed" toward formal AI-agent-facing status | [ADR-0002](../docs/foundation/adr/0002-ai-facing-docs-in-english.md) |
| 2026-07-16 | ADR-0005 (proposed) | Embedded-analytics BI product: serve dashboards dynamically through an authorization gate with layered caching (shell/result/authz, version-token invalidation, scope-normalized cache keys) instead of pure static SSG; hold tenant/role authorization as JWT claims backed by an epoch + denylist revocation mechanism for near-instant access cutoff | [ADR-0005](../docs/adr/0005-cache-and-authorization-architecture.md) |
| 2026-07-16 | LOG-0019 | Template governance profiles auto-discover one explicit parent chain rooted at `ai-dev-foundation`; required checks merge monotonically in foundation-to-repository order with stable cross-layer deduplication for migration compatibility | [ADR-0004](../docs/foundation/adr/0004-harden-multi-level-template-inheritance.md) |
| 2026-07-16 | ADR-0004 (accepted) | Repository owner approved the hardened direct-parent, manifest-owned, local-first inheritance architecture and solo-friendly governance defaults in PR #33 | [ADR-0004](../docs/foundation/adr/0004-harden-multi-level-template-inheritance.md) |
| 2026-07-16 | ADR-0004 (proposed) | Keep direct-parent template topology but replace denylist-only scheduled writes with manifest-owned, local-first inheritance; add inheritable template policy profiles, monotonic required checks, and solo-friendly defaults | [ADR-0004](../docs/foundation/adr/0004-harden-multi-level-template-inheritance.md) |
| 2026-07-16 | LOG-0018 | Superseding the fixed behavior in LOG-0003 and completing LOG-0017's migration condition, `scripts/setup-github.sh` is now a no-policy compatibility wrapper: dry-run delegates to `plan`, apply repeats and exactly matches the repository target before any GitHub read, and all exit codes and failures come from the reconciler | [.github/governance/README.md](../.github/governance/README.md) |
| 2026-07-16 | LOG-0017 | Public governance apply requires exact target confirmation before reads; the fixed setup path remains until vulnerability alerts and private vulnerability reporting have policy-owned adapters, preventing a security downgrade during migration | [.github/governance/README.md](../.github/governance/README.md) |
| 2026-07-16 | LOG-0016 | The internal governance execution boundary requires an exact target echo, executes one owned-field action at a time through local `gh`, verifies and replans from fresh state, and stops with redacted partial evidence rather than retrying or automatically weakening protections | [.github/governance/README.md](../.github/governance/README.md) |
| 2026-07-16 | LOG-0015 | Existing managed ruleset updates preserve supported stricter review, merge-method, and check-integration constraints; unknown or additional constraints fail closed instead of being overwritten | [.github/governance/README.md](../.github/governance/README.md) |
| 2026-07-16 | LOG-0014 | GitHub governance apply planning may create only the stable repository ruleset; it refuses existing-ruleset updates until stricter fields can be preserved, and uses documented API-only defaults for new rulesets | [.github/governance/README.md](../.github/governance/README.md) |
| 2026-07-15 | LOG-0013 | GitHub governance comparison identifies the managed ruleset as `ai-dev-foundation: branch-governance` and distinguishes compliant, drift, and permission-limited unknown state without treating unmanaged rules as owned | [.github/governance/README.md](../.github/governance/README.md) |
| 2026-07-15 | ADR-0003 (accepted) | Repository owner approved the layered GitHub governance policy and deterministic reconciler architecture in PR #19 | [ADR-0003](../docs/foundation/adr/0003-reconcile-github-governance-from-inherited-policy.md) |
| 2026-07-15 | ADR-0003 (proposed) | Replace the one-time GitHub governance bootstrap with a layered, rule-ID-mapped policy and deterministic plan/audit/apply reconciler; keep administrator apply credentials local, preserve downstream overrides, and reject any weakening of foundation MUST controls | [ADR-0003](../docs/foundation/adr/0003-reconcile-github-governance-from-inherited-policy.md) |
| 2026-07-13 | LOG-0012 | Ship a stack-agnostic Dev Container so agents run in a container, not on the host, keeping host credentials (`~/.ssh`, cloud configs) unreachable. `Co-Authored-By` AI trailers made a per-repo decision (WF-021): included by default here, disabled via a `commit-msg` hook on engagements where disclosure conflicts with the contract | [.devcontainer/README.md](../.devcontainer/README.md) |
| 2026-07-13 | LOG-0011 | Read-tool deny-list extended to `*.p12`/`*.pfx`, `~/.ssh`, `~/.config/gcloud`, `~/.gemini` to match the org Claude usage guide | [.claude/settings.json](../.claude/settings.json) |
| 2026-07-13 | LOG-0010 | GR-003 extended to the Bash path: `guard-bash.sh` blocks reading credential files to stdout (`cat .env`, `head *.key`, `cat ~/.ssh/*`) and full-environment dumps (`printenv`, bare `env`). Safe templates (`.env.example/.sample/.template`), single named vars (`printenv PATH`), and the `env FOO=1 cmd` prefix form stay allowed | [.claude/hooks/guard-bash.sh](../.claude/hooks/guard-bash.sh) |
| 2026-07-11 | LOG-0009 | GR-011 guard exempts floating-tag moves: a force push passes only when the command's single `git push` targets explicit `refs/tags/` refspecs (canonical form `git push --force <remote> refs/tags/<tag>`). Bare tag names stay blocked as branch-ambiguous; extra refspecs or a second push behind `&&`/`;` stay blocked | [.claude/hooks/guard-bash.sh](../.claude/hooks/guard-bash.sh) |
| 2026-07-10 | LOG-0008 | Deep-module interface design adopted as ARC-005 (interface = all a caller must know; deletion test; a seam needs a current second adapter; interface is the test surface). Glossary gains a banned-synonym *Avoid* column and a Resolved-ambiguities log, enforced via COD-002. Ideas from Ousterhout / Feathers, rewritten in this repo's rule system — not copied | [.ai/architecture.md](architecture.md) |
| 2026-07-03 | LOG-0007 | Markdown formatting MUST be frontmatter-aware: mdformat pinned via pre-commit with `mdformat-frontmatter` + `mdformat-gfm`, config in `.mdformat.toml` (`wrap=keep`, `number=true`). A naive run once collapsed all YAML frontmatter into headings — never use a formatter without these plugins | [.mdformat.toml](../.mdformat.toml) |
| 2026-07-02 | ADR-0002 | AI-facing docs are written in English | [ADR-0002](../docs/foundation/adr/0002-ai-facing-docs-in-english.md) |
| 2026-07-02 | ADR-0001 | Record architecture decisions as ADRs | [ADR-0001](../docs/foundation/adr/0001-record-architecture-decisions.md) |
| 2026-07-02 | LOG-0006 | `guard-bash.sh` must work when `jq` is absent (the `\|\| cat` fallback greps raw hook JSON); GR-010/011 patterns therefore treat `"` as a token terminator. Do not "simplify" that away. Verified by a matrix test on both paths | — |
| 2026-07-02 | LOG-0005 | AI PR review runs via `ai-review.yml`, disabled by default (repo var `ENABLE_AI_REVIEW`); supplements, never replaces, human review | — |
| 2026-07-02 | LOG-0004 | Template updates distribute via actions-template-sync PRs; downstream-customized files protected by `.templatesyncignore` | — |
| 2026-07-02 | LOG-0003 | GitHub governance (branch protection etc.) bootstrapped by `scripts/setup-github.sh` (gh CLI, idempotent) instead of a Probot app — no extra runtime dependency | — |
| 2026-07-02 | LOG-0002 | Canonical make targets are a binding contract (check-only lint, no `%:` catch-all, GR-031-guarded destructive targets); stack examples live in `profiles/` | [profiles/README.md](../profiles/README.md) |
| 2026-07-02 | LOG-0001 | Skills are vendor-neutral files in `.skills/`, routed via CLAUDE.md table instead of duplicated `.claude/skills/` wrappers | — |
